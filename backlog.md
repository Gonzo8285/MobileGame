# Gaming app — research & dev backlog

Heartbeat reads top-most `[ ]` item, does ONE bite-sized chunk (≤10 min of work), appends findings to `research_notes.md`, ticks the item, then stops. Keeps each run cheap.

## Phase 0 — Infrastructure verification (do these FIRST when sandbox is online)

- [x] V1. Verify GitHub PAT works against Gonzo8285/MobileGame. Use the Read tool with this EXACT absolute path to load the token: `C:\Users\Paul McCann\OneDrive - R.T. Keedwell Group\Documents\Claude\Projects\Gaming app\secrets\github_pat.txt`. Then from the bash sandbox call `GET /repos/Gonzo8285/MobileGame` and `git ls-remote https://x-access-token:$TOKEN@github.com/Gonzo8285/MobileGame.git`. Append result to research_notes.md and update project_config.md "Verified" line. If sandbox unavailable, leave V1 unticked and try again next run. If 401/403/404, flag `**BLOCKED — needs Paul:** PAT scope or repo access` at top.
- [x] V2. Once V1 passes, write a minimal `.gitignore` (covers `secrets/`, OS junk, engine build dirs) and `README.md` to the repo, commit "chore: bootstrap repo", push to main. Confirms write access. (Done via GitHub web UI 2026-04-29 — sandbox proxy blocks github.com and Paul's work network does TLS interception, so CLI push not viable from either side. Files authored locally in project folder, then uploaded by Paul in browser.)

## Phase 1 — Research (do these first, in order)

- [x] R1. Top-grossing & most-downloaded mobile genres on iOS+Google Play, 2026 — list 5 genres with one-line "why it makes money" each
- [x] R2. Hybrid-casual vs hyper-casual vs casual vs midcore — 4-line cheat sheet of dev cost, time-to-ship, ARPDAU range
- [x] R3. Top 10 most addictive mechanics (variable rewards, daily streaks, energy, gacha, battle pass, loss aversion, social proof, FOMO, sunk cost, near-miss) — one line each on how to implement
- [x] R4. Free game engines shortlist (Unity, Godot 4, Defold, Phaser, Cocos Creator) — score each on: free?, AI/CLI-controllable?, iOS+Android export, asset ecosystem
- [x] R5. Pick the recommended engine for Claude-driven dev with reasoning (≤6 lines) — **Godot 4**
- [x] R6. Free asset pipelines — sprites (Stable Diffusion / Kenney.nl / OpenGameArt), audio (freesound, ElevenLabs free, Suno free tier), fonts (Google Fonts) — one line each, licence note
- [x] R7. Monetization stack — AdMob vs AppLovin MAX vs Unity LevelPlay; rewarded video best practice; IAP price ladder template
- [x] R8. Retention systems blueprint — Day-1, Day-7, Day-30 mechanics; daily reward calendar template
- [x] R9. Apple Developer Program 2026 — cost, what Paul must do personally, what Claude can prep, signing requirements (must be on a Mac or use a cloud-Mac CI)
- [x] R10. Google Play Console 2026 — cost, internal/closed/open testing tracks, required artefacts, 14-day pre-launch checklist
- [x] R11. ASO 2026 — title/subtitle/keyword fields, screenshot frame templates, A/B test tooling
- [x] R12. Soft-launch playbook — recommended markets (PH, CA, NZ, etc.), KPIs to gate global launch
- [x] R13. CI/CD options Claude can drive headless — GitHub Actions, EAS Build, Codemagic, Unity Cloud Build — costs, free tiers, auth requirements
- [x] R14. AI tools/MCPs worth connecting — image gen, audio gen, analytics, store-listing — list and which are free
- [x] R15. Final research summary — 1-page condensed brief Paul reads before Phase 2 — see `phase1_brief.md`

## Phase 2 — Game selection (UNLOCKED 2026-04-28)

References + constraints captured in research_notes.md. Heartbeat: do R3–R8 first, then proceed to G1.

- [x] G1. Unpack Paul's game references — done in main session (see "Phase 2 unlocked" block in notes)
- [x] G2. Concept selection — Paul picked #1 directly (no need for full pitch comparison)
- [x] G3. **PAUL PICKED:** Roguelite TD deckbuilder, grimdark fantasy, original IP
- [x] G4. 1-page Game Design Doc — core loop, run structure, card types, Warlord classes, faction list (original names), monetisation surfaces, MVP scope, working title shortlist — see `gdd_v0.md`
- [x] G5. Name the game — 5 working-title candidates in the grimdark "in the style of" lane, check trademark + .com + app-store availability for each — see `g5_names.md` (top pick: Gallowmarch, awaiting Paul's call)
- [x] G6. Faction bible — 4–5 original factions with one-line vibe + visual cue (e.g. "Iron Penitents = zealot crusaders, hammer + chain motif"). NO GW/WotC names. — see `faction_bible.md`
- [x] G7. Card design v0 — 30-card starter pool across 3 factions: 18 units, 8 spells, 4 traps. Costs, rarities, simple keywords. — see `cards_v0.md`
- [x] G8. Monetisation surface map — exactly where microtxns, bundles, energy, resource locks, battle pass slot in. One diagram in markdown.
- [x] G9. **Warlord roster — 10 characters.** 5 free at start, 5 unlockable. Each gets: name, faction, one-line backstory, signature passive, signature unit, signature spell, visual cue. Free 5 should span playstyles (aggro / control / swarm / summoner / tempo) so new players can find their fit. Paid 5 should be flashier/synergistic to drive desire — but NOT pay-to-win. Note unlock cost/method per paid Warlord. — see `warlords_v1.md` (Gallowfell-aligned respin of v0)

## Phase 2.5 — Gallowfell lore reconciliation (added 2026-04-30 after lore_gallowfell.md)

Paul reframed factions/setting in `lore_gallowfell.md`. v0 design docs use the old faction names; sync them so Phase 3 builds against the canonical lore.

- [x] S1. Update `faction_bible.md` to Gallowfell factions: keep Iron Penitents; replace with Withered Court (Catacombs), Hollow Pact (Bog of Bargains), Ferrum Host (Foundry), Sable Wilds (Cinderwood). Pull motivation + visual motif from `lore_gallowfell.md`.
- [x] S2. Update `gdd_v0.md`: swap faction list, add Hanging Hour + Reanimation as core mechanics, add biome map, swap working-title block to "The Curse of Gallowfell".
- [x] S3. Update `cards_v0.md` faction tags to new faction names; spot-check card flavour text for references that now break.
- [x] S4. Trademark + .com + Apple/Google store availability scan on "Gallowfell" — surface-level WebSearch returns no public hits. Formal USPTO/EUIPO/UKIPO + domain registrar check still owed; queue for Claude Code on Paul's laptop (it can hit those endpoints). _(Update 2026-05-02 — Claude Code ran the scan: all 4 candidate domains (gallowfell.com/.app/.io/.game) NXDOMAIN, zero web footprint anywhere. USPTO/EUIPO/UKIPO programmatic queries blocked — bots not allowed. **Remaining gate: Paul must log in manually to tmsearch.uspto.gov, euipo.europa.eu/eSearch, and trademarks.ipo.gov.uk to confirm no registered marks before locking the title.**)_

## Phase 2.6 — Deck depth expansion (added 2026-04-30 by Paul)

Original 30-card pool too thin for real deckbuilding. Target: ~35-40 cards per faction × 5 = 175-200 launch cards. Each faction supports 3 sub-archetypes (~10-12 cards each), synergy anchored on 2-3-4-cost cards.

- [x] C1. Per-faction archetype briefs — for each of the 5 factions, define 3 sub-archetypes with: identity card (a 4-cost rare that names the strategy), payoff card, supporting cast 2-3-4 cost spine, and 1 anti-synergy with a rival faction archetype. Output: `archetypes_v0.md`.
- [x] C2. Iron Penitents full pool (~40 cards) — Bleed-stack / Sacrifice-Penance / Cleave-melee. 60% units, 25% spells, 10% traps, 5% specials. Output: `cards_iron_penitents_v1.md` + `.tres` files in `game/data/cards/iron_penitents/`. _(Done 2026-05-01: markdown authored in earlier live session; .tres P10–P40 batch-generated this heartbeat. P39/P40 relics flagged as `is_draftable=false` + `unlock_tag=&"relic_iron_penitents"` pending Paul's confirmation on the relic-slot system per cards_iron_penitents_v1.md open Q1.)_
- [x] C3. Ash-Mourners full pool (~40 cards) — Smoke-Fear control / Resurrect spam / Trap-control. Same structure. _(Done 2026-05-01: `cards_ash_mourners_v1.md` authored + 29 new `.tres` files (M12–M40) under `game/data/cards/ash_mourners/` + `M5.tres` reshaped in place per S2 promotion. Two relics flagged `is_draftable=false`+`unlock_tag=&"relic_ash_mourners"` pending Paul's relic-system call.)_
- [x] C4. Coven of the Black Mire full pool (~40 cards) — Poison-stack / Bog-Spawn swarm / Sacrifice combo. _(Done 2026-05-01: `cards_coven_v1.md` authored + 30 new `.tres` files (C11–C40) under `game/data/cards/coven/`. C6 Mother Quag retained as single dual-archetype card per Paul's lock. Two relics flagged `is_draftable=false`+`unlock_tag=&"relic_coven"` pending Paul's relic-system call.)_
- [x] C5. The Last Legion full pool (~40 cards) — Rally-Formation / Tempo-Echo / Banner-buff. _(Done 2026-05-01 heartbeat: `cards_last_legion_v1.md` authored + 40 new `.tres` files (L1–L40) under `game/data/cards/last_legion/`. Net-new faction (no v0 cards). 4 Rares = Sergeant-Smith Vikar (L7), Echo-Sergeant (L18), Banner-Captain of the Crowned Anvil (L33), Crowned Anvil Standard 5c artifact-unit (L34). 60/25/10/5 distribution + 24/12/4 rarity hit exactly. Echo flagged as soft-keyword for engine-side enum addition (Q1 — affects B2.5 wave-spawner). Banner-Token flagged as new lane-object class (Q2 — affects B2.6/B2.7). Two relics flagged `is_draftable=false`+`unlock_tag=&"relic_last_legion"` pending Paul's relic-system call.)_
- [x] C6. Skinward Pact full pool (~40 cards) — Big-monster / Transformation / Beast-summon. _(Done 2026-05-02 heartbeat: `cards_skinward_pact_v1.md` authored + 40 new `.tres` files (W1–W40) under `game/data/cards/skinward_pact/`. Net-new faction (no v0 cards). 4 Rares = Bear-Skin Hierophant (W4 E1 identity), Thrask the Bear-Who-Was-King (W8 E1 6c R payoff), Wyrd-Shifter of the Cinderwood (W19 E2 identity), Pelt-Bound Shaman (W31 E3 identity). 60/25/10/5 distribution + 24/12/4 rarity hit exactly. W27 Cub-Token + W28 Wolf-Token authored as 0c C draftable (mirrors C1 Bog-Spawn pattern). Transformation flagged as soft-keyword on 9 cards for engine-side enum addition (Q1 — affects B2.6/B2.7 card-play loop). Wilds-tag system flagged for engine work (Q2 — needs faction-4 lookup OR new tags field on Card). Two relics flagged `is_draftable=false`+`unlock_tag=&"relic_skinward_pact"` pending Paul's relic-system call.)_
- [x] C7. Cross-faction balance pass — ensure each archetype has a clean counter elsewhere; flag any combos that snowball without a check; rarity distribution consistent across factions. _(Done 2026-05-02 04:17 UTC: rarity 24C/12U/4R verified consistent across all 5 factions; v0.1 anti-synergy grid re-validated symmetric (3-deal/3-receive each); 3 snowball flags raised — aura-stacking rule needs adding to GDD, Bleed-5 per-enemy cap recommended, Wyrd-Shifter ramp flagged for soft-launch monitor. None require card redesign — all 1-line GDD clarifications. Pool is internal-MVP ship-ready. See research_notes.md "C7 — Cross-faction balance pass" for full audit.)_
- [x] C8. Internal-MVP scope update — first internal build = 3 factions × ~100 cards (Penitents + Mourners + Coven). Validate deckbuilder feel before adding the other 2. _(Done 2026-05-02 heartbeat: `internal_mvp_scope.md` authored. IMV-1 = 3 factions + 3 faction-locked Warlords + B2.5–B2.10 build slice. Tier system (W1–W5), real ad/IAP SDK (B4), AI art (B3), soft-launch CI (B5–B7) all deferred to IMV-2. Last Legion + Skinward Pact `.tres` stay on disk, flipped `is_draftable=false`. 5-criterion validation gate defined. Open Q for Paul on Warlord trio composition — non-blocking, default = canonical free-3.)_

## Phase 2.11 — AI art pipeline setup + spec population (added 2026-05-02 — see `pipeline_spec.md`)

Pipeline architecture, prompt template, and per-card spec format all locked. Autonomous heartbeat work to make the pipeline real and populate the ~220 per-card spec files. Each item is heartbeat-sized; do them in order.

- [x] D-LORA-1. **Civitai LoRA URL hunt + verification.** For each LoRA listed in `pipeline_spec.md` §2.2 (style + 5 faction-specific), do a WebSearch / WebFetch to find the canonical Civitai URL. Verify each URL returns 200. Record `name → URL → version` in a new file `pipeline_setup/loras_resolved.md`. If a LoRA name doesn't map to anything, propose a closest-match substitute (e.g. "mtg-style-sdxl" → search for "magic the gathering style sdxl lora", pick highest-rated, log substitution rationale). Update `pipeline_spec.md` §2.2 with final names. Update `pipeline_setup/README.md` step 4 with the URLs. **Stop condition:** all 8 LoRAs have a verified URL or a documented substitution. _(Done 2026-05-04 14:18 UTC heartbeat: `pipeline_setup/loras_resolved.md` authored — 8 slots resolved across 9 LoRAs, 3 IP-blocked slots (mtg/phyrexian/lorwyn) substituted with documented rationale. `pipeline_spec.md` §2.2 + `pipeline_setup/README.md` step 4 updated with final names + Civitai URLs. 4 follow-ups deferred: license audit, trigger-word capture, version pinning, backup verification — none block first 9-tile reference sheet.)_
- [x] D-WORKFLOW-1. **Author the ComfyUI workflow JSON** at `pipeline_setup/workflow_gallowfell_card.json`. Use ComfyUI's standard node graph: CheckpointLoaderSimple → multiple LoraLoader nodes (one per LoRA from D-LORA-1) → CLIPTextEncode (positive prompt) + CLIPTextEncode (negative prompt) → KSampler (DPM++ 2M Karras, 30 steps, CFG 6.5) → VAEDecode → SaveImage. Hardcode the locked sampler + step count + CFG. Leave prompt + LoRA weights as the only player-editable fields. Reference structure: search ComfyUI examples GitHub for "SDXL with multi-LoRA workflow" and adapt. _(Done 2026-05-04 21:30 UTC heartbeat: 17 nodes / 29 links — 1 Checkpoint + 10 LoraLoader chain + 2 CLIPTextEncode + EmptyLatentImage + KSampler + VAEDecode + SaveImage. Locked widgets: dpmpp_2m / karras / 30 steps / CFG 6.5 / 832×1216 / fixed seed 4242 / juggernautXL_v9. Full §3.6 negative prompt baked in. Player-editable: positive prompt + per-LoRA weights (faction-zeroing protocol documented in workflow's `extra.lora_weight_protocol` field). Subagent validated: JSON parses cleanly, all 29 link cross-references consistent in both directions. Filename gotcha — if Civitai downloads land with different filenames than the workflow's defaults, rename the .safetensors files rather than editing the workflow (preserves reproducibility). D-WORKFLOW-2 SD 1.5 fallback still queued.)_
- [x] A-SPEC-1. **Populate per-card art spec files for Iron Penitents.** Read `cards_iron_penitents_v1.md` + `lore_gallowfell.md`. For each of ~40 cards (P1-P40), author `art_specs/iron_penitents/<card_id>_<lower_snake_name>.md` following `art_specs/_template.md`. Each spec must: (1) extract subject description from the card's design doc, max 30 words, (2) assemble the resolved prompt by concatenating §3.1 + §3.2-IronPenitents + §3.3-this-card + §3.4 + §3.5 from `pipeline_spec.md`, (3) pin the seed deterministically per the rule in `_template.md` (commons: digit-stripped id × 100 + faction tag). Don't deviate from defaults. **Stop condition:** all P1-P40 have a spec file.
- [x] A-SPEC-2. **Populate Ash-Mourners spec files.** Same pattern, ~40 cards, M1-M40, into `art_specs/ash_mourners/`. _(Done 2026-05-05 heartbeat: 40 spec files written under `art_specs/ash_mourners/m1_…m40_…`. UNIT cards (24) use §3.4 portrait composition; SPELL/TRAP/RELIC cards (16) use §3.4 environment composition. Subject descriptions ~25–30 words, pulled from `cards_ash_mourners_v1.md` mechanic + `lore_gallowfell.md` motif vocabulary (court-shroud, raven-quill, censer-smoke, catacomb-vault, dusk-purple). Faction LoRA stack: ClassipeintXL@0.8 + Dark Fantasy@0.8 + Elden Ring Style@0.5 + gothic cathedral interior@0.6 + Dark Gothic Fantasy@0.5 (per pipeline_spec.md §2.2). Seed formula = 50000 + 101 × N (Ash-Mourners faction tag = 5; mirrors Iron Penitents' 40000+101×N pattern, no collisions). Validation checklist includes faction-specific palette check ("dusk-purple accent, not blue or fuchsia"). Generated by `/tmp/gen_ash_specs.py` — script not committed; per-card spec files are the source of truth.)_
- [x] A-SPEC-3. **Populate Coven of the Black Mire spec files.** ~40 cards, C1-C40, into `art_specs/coven/`. _(Done 2026-05-08 09:20 UTC heartbeat: 40 spec files written under `art_specs/coven/c1_…c40_…`. UNIT cards (24) use §3.4 portrait composition; SPELL/TRAP/RELIC cards (16) use §3.4 environment composition. Subject descriptions ~25–30 words, pulled from `cards_coven_v1.md` mechanics + `lore_gallowfell.md`/`pipeline_spec.md` §3.2 motif vocabulary (swamp-witch, demon-coin, three-shadows-cast, green pyre eyes, briar-tangled cloak, bog-green accent, fungal-grove, lorwyn-folkloric grotesque). Faction LoRA stack: ClassipeintXL@0.8 + Dark Fantasy@0.8 + Elden Ring Style@0.5 + Swamp people@0.5 + Mythical Forest Style [SDXL]@0.5 (per `pipeline_spec.md` §2.2). Optional Witch Style @ 0.4 left out of default stack — open Q raised. Seed formula = 60000 + 101 × N (Coven faction tag = 6); range 60101–64040, no collisions with prior factions. Validation checklist palette line set to "bog-green accent, not lime green or neon"; body-horror line broadened to "Phyrexian / lorwyn-grotesque undertone." RELIC card_type used for C39/C40 (matches Ash-Mourners convention). Generated by `outputs/gen_coven_specs.py` — script not committed; per-card spec files are the source of truth.)_
- [x] A-SPEC-4. **Populate The Last Legion spec files.** ~40 cards, L1-L40, into `art_specs/last_legion/`. _(Done 2026-05-08 heartbeat: 40 spec files written under `art_specs/last_legion/l1_…l40_…`. UNIT cards (24) use §3.4 portrait composition; SPELL/TRAP/RELIC cards (16) use §3.4 environment composition. Subject descriptions ~25–30 words, pulled from `cards_last_legion_v1.md` mechanics + `pipeline_spec.md` §3.2 Last Legion motif vocabulary (soot-blackened cuirass, brass officer gorget, ironwood baton, chain-bound hair, foundry-rivet armour, decayed-knight elden-ring scale, brass accent, smoke-and-coal background). Faction LoRA stack: ClassipeintXL@0.8 + Dark Fantasy@0.8 + Elden Ring Style@0.5 + ArmorSentinel medieval armor style@0.6 (per `pipeline_spec.md` §2.2). Seed formula = 70000 + 101 × N (Last Legion faction tag = 7); range 70101–74040, no collisions with prior factions (Penitents 40101–44040 / Mourners 50101–54040 / Coven 60101–64040). Validation checklist palette line set to "brass accent, not gold or copper"; body-horror line softened to "decayed-knight elden-ring scale present, no explicit gore" (Last Legion is the least body-horror of the five — they're decayed knights, not flagellants/witches). RELIC card_type used for L39/L40 (matches Penitents/Mourners convention). Echo soft-keyword visible in subject text on Echo-archetype cards but no engine impact (text-only, per cards_last_legion_v1.md Q1). Generated by `outputs/gen_last_legion_specs.py` — script not committed; per-card spec files are the source of truth.)_
- [x] A-SPEC-5. **Populate Skinward Pact spec files.** ~40 cards, W1-W40, into `art_specs/skinward_pact/`. _(Done 2026-05-08 heartbeat: 40 spec files written under `art_specs/skinward_pact/w1_…w40_…`. UNIT cards (24) use §3.4 portrait composition; SPELL/TRAP/RELIC cards (16) use §3.4 environment composition. Subject descriptions ~25–30 words, pulled from `cards_skinward_pact_v1.md` mechanics + `pipeline_spec.md` §3.2 Skinward Pact motif vocabulary (antler-crown sprouting through bone, hide cloak layered, mismatched eyes, smoke-trailing fingers, druidic shaman with phyrexian undertones, bark-brown accent, cinderwood-grove background). Faction LoRA stack: ClassipeintXL@0.8 + Dark Fantasy@0.8 + Elden Ring Style@0.5 + Mythical Forest Style [SDXL]@0.5 + Mythical Creatures SDXL@0.5 (per `pipeline_spec.md` §2.2; Mythical Forest is shared with Coven). Seed formula = 80000 + 101 × N (Skinward Pact faction tag = 8); range 80101–84040, no collisions with prior factions (Penitents 40101–44040 / Mourners 50101–54040 / Coven 60101–64040 / Last Legion 70101–74040). Validation checklist palette line set to "bark-brown accent, not red-brown or olive"; body-horror line set to "druidic-with-phyrexian-undertones present, no explicit gore" (Skinward Pact's Phyrexian undertone is faction-canonical per §3.2). RELIC card_type used for W39/W40 (matches Penitents/Mourners/Coven/Last Legion convention). Transformation soft-keyword visible in subject text on transformation-archetype cards but no engine impact (text-only, per cards_skinward_pact_v1.md Q1). W27 Cub-Token + W28 Wolf-Token authored as draftable 0c C — UNIT spec (mirrors C1 Bog-Spawn pattern from Coven). Generated by `outputs/gen_skinward_specs.py` — script not committed; per-card spec files are the source of truth. Phase 2.11 spec-file population for the 5 factions × 40 cards is now complete; A-SPEC-6 (Warlords) and A-SPEC-7 (enemies) still queued.)_
- [x] A-SPEC-6. **Populate Warlord spec files.** All 11 Warlords from `warlords_v1.md`, into `art_specs/warlords/<warlord_id>_<name>.md`. These are flagship images; spec accordingly with composition tier "warlord_signature" and seed = warlord_number × 10000 + 4242. _(Done 2026-05-09 heartbeat: 11 spec files written under `art_specs/warlords/w1_…w11_…`. card_type=WARLORD, rarity=LEGENDARY, role=warlord_signature on all. Subject descriptions ~25–30 words pulled from `warlords_v1.md` backstory + visual cue lines. Composition uses §3.4 portrait variant (warlords are character cards). Seed formula = N × 10000 + 4242 (Vyrrun=14242 … Saint=114242), all clear of the 5 faction common-card seed ranges (verified: 44242 > 44040, 54242 > 54040, 64242 > 64040, 74242 > 74040, 84242 > 84040). Faction style §3.2 + LoRA stack assigned per primary faction: W1 iron_penitents, W2 ash_mourners, W3 coven, W4 last_legion, W5 skinward_pact. Hybrids W6/W8/W9 use primary-faction LoRA + secondary-faction motif via subject text + a documented deviation note (W6 Penitents-primary, W8 Coven-primary, W9 Skinward-primary) — escalation path to dual-LoRA stack flagged if the first pass under-reads. Neutral/curse-bound W10 (Last Confessor) + W11 (Saint That Should Not Hang) mapped to Ash-Mourners as closest-existing pipeline_spec faction style with deviation notes; W11 includes a sharper PEGI-12 guard (rope-mark must read as iconography, not injury). All warlords have idle_loop_frames=4 (per art_direction.md §3) + alt_arts=["mastery_skin"] for the Tier-4 mastery cosmetic. output_path=`art/warlords/<wN>_<snake>.png`. Generated by `outputs/gen_warlord_specs.py` — script not committed; per-warlord spec files are the source of truth. A-SPEC-7 (5 placeholder enemies) still queued.)_
- [x] A-SPEC-7. **Populate enemy spec files.** All 5 placeholder enemies from `game/data/enemies/`, into `art_specs/enemies/`. Plus future bosses as they're defined. _(Done 2026-05-09 04:19 UTC heartbeat: 5 spec files written under `art_specs/enemies/e1_…e5_…`. card_type=ENEMY, role=standard. Subject descriptions ~25–30 words pulled from each enemy's `.tres` flavour + `pipeline_spec.md` §3.2 faction motif vocabulary. Faction routing follows enemy.faction enum: E1 (Penitents stack) / E2,E3 (Coven stack) / E4,E5 (Ash-Mourners stack). E5 Reaper-Bell uses §3.4 environment-led composition (siege-object, not humanoid) — only deviation, documented in spec. Seed formula = 90000 + 101 × N (Enemy tag = 9, distinct from all 5 faction common-card seeds + Warlord N×10000+4242 ranges, no collisions). Rarity assigned for art-tier: E1–E3 COMMON / E4 UNCOMMON / E5 RARE. idle_loop_frames=2 (commons-tier convention). Phase 2.11 spec population now complete: 5 factions × 40 + 11 Warlords + 5 enemies = 216 spec files total. D-WORKFLOW-2 + D-VALIDATE-1 still queued.)_
- [x] D-WORKFLOW-2. **Author the ComfyUI workflow variant for SD 1.5 fallback** at `pipeline_setup/workflow_gallowfell_card_sd15.json`. Same structure, swap checkpoint loader to SD 1.5, drop SDXL-specific LoRAs, retain the prompt/negative architecture so spec files run unchanged. _(Done 2026-05-09 heartbeat: 7 nodes / 9 links — CheckpointLoaderSimple (v1-5-pruned-emaonly) → 2× CLIPTextEncode (positive + LOCKED negative, prompt scaffolds identical to SDXL workflow modulo `8k`→`highres` tag) → EmptyLatentImage (512×768 native SD 1.5 portrait, matches 2:3 ratio of SDXL's 832×1216) → KSampler (dpmpp_2m / karras / 30 / CFG 6.5 / seed 4242 — all locked params matched to SDXL workflow for spec-file portability) → VAEDecode → SaveImage (`gallowfell/card_sd15` prefix). LoRA stack dropped entirely — 4 of the 10 SDXL LoRAs are SDXL-explicit by name and the remaining 6 have unverified SD 1.5 availability; running base SD 1.5 with prompt-only style targeting is the cheap path. Suggested operator-swappable finetunes documented in `extra.expected_checkpoint` (RealisticVision / DreamShaper / RevAnimated / AbsoluteReality). Subagent validated: JSON parses, all 9 links cross-reference symmetrically, KSampler has all 4 required inputs, SaveImage IMAGE input wired. Spec files under `art_specs/` paste into node 2 unchanged. Future task to queue only if real operator hits this fallback AND base output proves insufficient: `D-LORA-SD15: research SD 1.5 LoRA equivalents`. Cheapest path = defer.)_
## Phase 2.16 — Deck-Builder screen (added 2026-06-01 by Controller; Paul-directed)

Player-facing pre-run screen to choose which cards go into the deck. **Full build-ready spec + GDScript
scaffold in `DECKBUILDER_SPEC_2026-06-01.md` (repo root).** v0 rules: 20-card singleton deck, faction-locked
+ Neutral, tokens/relics (`is_draftable=false`) excluded. Slots between Warlord-select and `start_run`.
Work top-down; each chunk is one heartbeat. Verify on a desktop Godot run per the spec's §8 acceptance list.

- [x] DB-1. **CardDatabase autoload.** Add `src/runtime/card_database.gd` (scan `res://data/cards/**`, index by id + faction; `draftable_for()`, `get_by_id()`, `resolve_deck()`), register as autoload `CardDatabase` after `GameState` in `project.godot`. Code in spec §3. Smoke: print pool counts per faction at boot. _(Done 2026-06-25, commit 9fc3cad: card_database.gd authored + autoload registered after GameState; get_by_id/draftable_for/resolve_deck + CO-1 accessors all_cards/by_faction/counts_by_faction; boot smoke prints "indexed 207 cards across 5 factions — ASH=41 COVEN=42 IRON=41 LEGION=41 SKINWARD=42"; card_database_test.gd wired into main.gd, suite 10/10 green. Hardened vs spec: no get_or_add, also scans loose root .tres + .tres.remap.)_
- [x] DB-2. **GameState deck persistence.** Add `last_deck_ids: Array[StringName]` to the persisted profile + `set_last_deck()` + `start_run_from_ids()` wrapper (resolves ids→Cards via CardDatabase, calls existing `start_run`). Code in spec §4. Leave `start_run(Array[Card])` untouched. _(Done 2026-06-25, commit 9067daa: last_deck_ids + set_last_deck() (defensive copy) + start_run_from_ids() wrapper; start_run untouched. NOTE: no profile/disk-save system exists in the codebase yet (all GameState state is in-memory), so storage is in-memory and cross-session restore (acceptance #7) is deferred to IMV-2 with a real save system. game_state_test.gd extended; suite 10/10 green.)_
- [x] DB-3. **deck_builder.gd core logic.** Add/remove, `DECK_SIZE=20`, `MAX_COPIES=1`, `_can_add`, `_passes_filter`, `_on_start` (writes ids → `set_last_deck` + `start_run_from_ids` + `enter_chapter(1)`), Auto-fill, Clear. Code in spec §5 (logic only; UI in DB-4). _(Done 2026-06-26, commit c373a27/860fdf7: DeckBuilder model — add/remove, 20-card singleton, type filter + search, unit-biased Auto-fill, Clear, _on_start. UI-agnostic, exercised headless by deck_builder_test.)_
- [x] DB-4. **deck_builder.tscn + UI.** Root Control + script; programmatic UI matching `title.gd` convention: collection grid (tap=add), deck list (tap=remove), `%CountLabel` "N/20", filter chips (All/Units/Spells/Traps) + search, Back / Auto-fill / Clear / Start (disabled until 20/20). Layout in spec §6. Reuse `scenes/card_view.tscn` for tiles with placeholder art fallback. _(Done 2026-06-26: programmatic UI built in code (title.gd convention, no fragile .tscn tree) — grid + deck list + N/20 count + filter chips + search + Back/Auto-fill/Clear/Start-disabled-until-20 + validation text. Deviation: lightweight tap-to-add Button tiles (faction-tint) instead of combat CardView — CardView subscribes to GameState.mana and dims by affordability + does lane drag-drop, neither fits a builder. Art-in-tile + mana-curve histogram + long-press zoom = DB-7.)_
- [x] DB-5. **Wire `title.gd` → deck builder.** Replace `_start_run_with` body to instantiate `deck_builder.tscn` with `setup(warlord_id, faction)` instead of auto-starting; keep the old random-deck logic behind the builder's Auto-fill button. Code in spec §7. Add a `_faction_for_warlord()` helper (promote `run_controller._get_active_warlord_faction` mapping). _(Done 2026-06-26, commit 860fdf7: title emits new GameState.deck_build_requested → RunController swaps to the builder (setup() BEFORE add_child so _ready loads the right faction). `_faction_for_warlord` added; `_get_active_warlord_faction` extended to all 5 title Warlords (WL-5 will unify via WarlordDatabase). NOTE spec §7's `current_scene.queue_free()` did NOT fit — title is RunController's child, not current_scene — so used the signal/phase path instead. Verified by deck_builder_flow_test: Title→builder→run, phase MAP + 20-card deck.)_
- [x] DB-6. **deck_builder_test.gd (headless).** Assert `draftable_for` excludes non-draftables, deck-size + singleton rules enforced, `resolve_deck` round-trips ids→Cards. Add to the dev-test runner in `main.gd`. _(Done 2026-06-26: deck_builder_test.gd (model — faction-lock + draftable-only pool, singleton + size caps, autofill=20, _deck_ids→resolve_deck round-trip, _on_start GameState wiring) + deck_builder_flow_test.gd (full Title→builder→run scene integration). Both wired into main.gd; suite 12/12 green.)_
- [~] DB-7. **Polish.** Mana-curve mini-histogram, validation text ("Need N more" / "Max copies reached" / "20/20 — ready"), long-press card zoom via `card_view`, "Use last deck" shortcut from `GameState.last_deck_ids`. _(MOSTLY DONE 2026-07-08, commits 0da4a4b + earlier: validation text (shipped in DB-4), "Use Last Deck" button (faction-filtered), mana-curve display (cost:count buckets) — all tested. STILL TODO: long-press card zoom (wire once the shared card_detail overlay is factored out of collection.gd per CO-3).)_

## Phase 2.17 — Warlord-Select screen (added 2026-06-01 by Controller; Paul-directed)

The roster screen the player lands on for a new run; pick a Warlord → Deck Builder (Phase 2.16). **Full
spec in `WARLORDSELECT_SPEC_2026-06-01.md` (repo root).** Builds on `warlord_select_ui_v0.md` §2 (Screen A)
+ `warlords_v1.md` §Free roster. v0 = roster grid + light detail panel + Select→deck builder; tier modals /
XP strip / purchase flow deferred to IMV-2. Do AFTER the DB chunks (warlord-select calls `deck_builder.setup`).

- [x] WL-1. **Author/normalise the 5 free Warlord `.tres`** (vyrrun, sieren, eddra, veska, mhar) from `warlords_v1.md` §Free roster + spec §3 table — id, display_name, faction, base_passive_id, signature_unit/spell (point at matching card `.tres` by name→id if present, else null + note). Supersede the stale `vey.tres`/`quag.tres`. Keep `is_free=true`. _(Done 2026-06-26, commit c025778: vyrrun kept; sieren/eddra/veska/mhar authored — id/display_name/faction(1/2/3/4)/base_passive_id(hanged_memory/brood/forge_heat/wear_the_dead), is_free=true. signature_unit/spell left null per spec §8.1 (link when named cards resolved — flagged follow-up). Stale vey/quag ARCHIVED to data/warlords/_deprecated/ (preserved not deleted; WarlordDatabase scans top-level only).)_
- [x] WL-2. **WarlordDatabase autoload.** Add `src/runtime/warlord_database.gd` (scan `res://data/warlords/`, index by id; `get_by_id`, `faction_of`, `free_warlords`, `paid_warlords`), register after `CardDatabase` in `project.godot`. Code in spec §4. _(Done 2026-06-26, commit c025778: warlord_database.gd autoload registered after CardDatabase; get_by_id/faction_of(NEUTRAL fallback)/free_warlords(faction-sorted)/paid_warlords; skips `*_tiers.tres` + `_deprecated/`. Boot smoke: "indexed 5 warlords (5 free, 0 paid)".)_
- [x] WL-3. **warlord_select.gd + .tscn.** Screen-A roster grid: 5 free selectable tiles (art/name/faction/archetype) + paid tiles rendered locked w/ cost; tap free → detail panel (art + passive + signature names) → Select / Back. Code skeleton + layout in spec §5–6. _(Done 2026-07-08, commit 4af856a: warlord_select.gd/.tscn — data-driven roster grid from WarlordDatabase, free tiles selectable, paid rendered locked. Deviation: tap-tile = select (functional); the bigger detail panel + per-tile art are deferred polish.)_
- [x] WL-4. **Wire Title → Warlord Select → Deck Builder.** Title "Play" loads `warlord_select.tscn`; Select sets `GameState.active_warlord_id` + instantiates `deck_builder.setup(id, faction)`. Delete `title.gd`'s hardcoded warlord dict + auto-deck. Spec §5–6. _(Done 2026-07-08, commit 4af856a: GameState screen-router signals (warlord_select_requested/title_requested); Title single Play → Warlord Select; warlord grid removed from _build. Also fixed deck_builder Back (was change_scene_to_file → would replace RunController). Verified by warlord_select_flow_test.)_
- [x] WL-5. **Replace hardcoded faction map.** `run_controller._get_active_warlord_faction()` → `WarlordDatabase.faction_of(GameState.active_warlord_id)`. Removes the stale vyrrun/vey/quag match. _(Done 2026-07-08, commit 4af856a. Verified by warlord_select_flow_test — run_controller resolves ASH_MOURNERS for sieren via WarlordDatabase.)_
- [x] WL-6. **warlord_select_test.gd (headless).** Assert WarlordDatabase loads ≥5 free, `faction_of` matches spec §3 per id, `free_warlords().size() == 5`. Add to `main.gd` dev-test runner. _(Done 2026-06-26, commit c025778: warlord_select_test.gd wired into main.gd — free-5 loads, faction_of matches spec §3 table per id, free_warlords().size()==5, archived vey/quag excluded, unknown→NEUTRAL. Suite 13/13 green.)_

## Phase 2.18 — Card Collection / Codex screen (added 2026-06-01 by Controller; Paul-directed)

Read-only gallery to browse every card + read full detail; showcases the rendered art. **Full spec in
`COLLECTION_SPEC_2026-06-01.md` (repo root).** Reuses `CardDatabase` + `card_view`; no deck mutation, no
monetisation. Only hard dep is DB-1 (CardDatabase). Can build in parallel with the run flow.

- [x] CO-1. **CardDatabase accessors.** Add `all_cards()`, `by_faction(faction)`, `counts_by_faction()` to `card_database.gd`. Code in spec §2. (Codex includes tokens/relics, unlike the deck-builder.) _(Done ahead of schedule in DB-1, commit 9fc3cad: all three accessors on CardDatabase; by_faction returns a defensive copy.)_
- [x] CO-2. **collection.gd + .tscn.** Faction tabs (Iron/Ash/Coven/Legion/Skin/Neutral/All) + scrollable grid of `card_view` tiles + per-tab collected count header. Programmatic UI per `map_view.gd` convention. Layout in spec §3. _(Done 2026-07-08, commit 3164b11: collection.gd/.tscn — All+6 faction tabs, scrollable 5-col grid, per-view count. Lightweight tiles (not card_view — same rationale as deck-builder).)_
- [x] CO-3. **Shared `card_detail.tscn` overlay.** Big art, name/faction/rarity/cost, unit stats, keyword chips + `KEYWORD_BLURBS` tooltips, effect + flavour text, close. Built reusable so DB-7's card-zoom shares it. Spec §4. _(Done 2026-07-08, commit 3164b11 — detail overlay (name/faction/rarity/cost, unit stats, keyword names + blurbs, effect + flavour, Close) built IN-CODE in collection.gd. DEVIATION: not yet extracted to a shared card_detail.tscn — factor out when DB-7's long-press zoom lands so both share one view. Big art omitted pending per-card art in tiles.)_
- [~] CO-4. **Filters + search + locked rendering.** cost/type/rarity dropdowns + name search; tokens/relics get a badge; `unlock_tag` cards greyed w/ lock glyph (still openable). Spec §3–4. _(PARTIAL 2026-07-08, commit 3164b11: name search + faction tabs + token/relic badge shipped. STILL TODO: cost/type/rarity dropdown filters. Locked-glyph rendering is N/A at MVP — no `unlock_tag != ""` cards exist yet; wire when unlock economy lands (IMV-2).)_
- [x] CO-5. **Title wiring.** Add a "Collection" entry on the Title/shell → `collection.tscn`; Back → Title. Read-only. _(Done 2026-07-08, commit 3164b11: GameState.collection_requested router signal; RunController swaps to Collection; Title "Collection" button; Back → Title.)_
- [x] CO-6. **collection_test.gd (headless).** `all_cards().size()` == sum of per-faction `.tres`; `counts_by_faction()` sums match; detail overlay populates from a sample card. Add to `main.gd` dev-test runner. _(Done 2026-07-08, commit 3164b11: collection_test.gd wired into main.gd — all_cards==sum(counts_by_faction), by_faction defensive copy, screen builds + detail overlay opens on a sample. Suite 15/15 green.)_

## Phase 2.19 — Branching map + encounter variance (added 2026-06-01 by Controller; Paul rethink)

**SUPERSEDES the IMV-1 linear-gauntlet design call** — Paul 2026-06-01: linear felt bland / un-immersive.
Wants multiple possible journeys, variance in enemies + tactics, shown via different symbols. **Full spec in
`BRANCHING_MAP_SPEC_2026-06-01.md` (repo root).** Engine already supports branching `MapGraph` + node-kind
symbols; the new core is **Encounter Archetypes** (enemy+tactic flavour per node, each with a glyph). High
priority per Paul; independent of the DB/WL/CO blocks so can build in parallel.

- [x] BM-1. **EncounterArchetype + catalog.** Add `src/data/encounter_archetype.gd` (id, display_name, symbol, blurb, density, hp/atk/move bias, emphasise_keywords, prefer_faction) + the 7-archetype catalog from spec §2 (swarm/bruisers/plague/volley/ambush/summoners/dread). Add `encounter_id: StringName` to `MapNode` (+ `describe()`). _(Done 2026-07-08, commit 8a4d515: all 7 archetypes with unique symbols + biases; MapNode.encounter_id; encounter_test checks catalog integrity.)_
- [x] BM-2. **Branching generator.** `MapGenerator.generate_branching(chapter, seed)` — layered DAG (12 depths, 2–4 wide, converge to BOSS), node-kind distribution rules (spec §1), assign encounter_id to combat-type nodes varying along each path. Make `generate()` default to branching; keep `generate_linear()` for tests. `map_graph.validate()` must pass. _(Done 2026-07-08, commit cdbbd0b: 12-depth DAG, full SHOP+REST rows (per-path guarantee), ELITE gate ~d8, archetypes chosen to differ from every parent (no back-to-back), deterministic, validate() clean. generate()→branching; generate_linear() kept.)_
- [x] BM-3. **WaveGenerator.for_node.** `for_node(node, run_seed)` reads kind + encounter_id, applies archetype density/stat/keyword/move biases over round scaling, builds spawns. Keep `for_round` as a shim. Spec §2/§4. _(Done 2026-07-08, commit e324edb: depth scaling + archetype hp/atk/move/keyword/faction biases + density +/- spawns. encounter_test: legal wave per archetype, swarm out-numbers bruisers, deterministic. for_round kept.)_
- [x] BM-4. **map_view branching layout.** Render the graph as depth-columns with edges, current/visited/reachable states, **encounter symbols** on combat nodes (not generic ⚔), tap-locked to reachable children, name+blurb on tap, legend toggle. Spec §3. _(Done 2026-07-08, commit 79534a7: graph-driven depth rows + edges + encounter symbols + name/blurb tooltips + reachable=all-children tap-lock + current/visited/reachable states + legend. Builds cleanly headless via the flow tests; VISUAL LAYOUT still needs Paul's Godot editor pass. Legend is a static line, not a toggle — minor.)_
- [x] BM-5. **run_controller traversal.** Pick among `get_children(current)` per player tap; call `WaveGenerator.for_node`. Non-combat nodes keep auto-resolving (follow-on). Spec §4/§7. _(Done 2026-07-08, commit 79534a7: _wire_combat now calls WaveGenerator.for_node(cur_node, seed). Traversal (choose_next_node over get_children) + non-combat auto-resolve were already in _on_map_node_selected.)_
- [x] BM-6. **Tests (headless).** Extend `map_test.gd` (branching reachability, boss-terminal, per-path REST+SHOP guarantee, no back-to-back archetype repeats) + new `encounter_test.gd` (each archetype → legal wave; symbol+blurb present). Spec §6. _(Done 2026-07-08, commits cdbbd0b + e324edb: map_test rewritten for branching (validate clean, full SHOP/REST rows, ELITE gate, no back-to-back, determinism, GameState nav walks children→boss) + generate_linear back-compat check; encounter_test covers catalog + for_node legal-wave-per-archetype. Suite 17/17 green.)_

- [ ] D-VALIDATE-1. **Pipeline validation pass — Warlord-anchor first, commons second.** Two-stage per `pipeline_spec.md` §5 (rewritten 2026-05-11). **Stage A:** generate the 5 free Warlords (W1 Vyrrun / W2 Sieren / W3 Eddra / W4 Veska / W5 Mhar) — outputs to `art_iterations/_anchors/`. Paul + Cowork co-validate against §5 Stage-A checklist; failing tiles regenerate before Stage B fires. Once signed off, anchors copy to `art/warlords/` AND `art_specs/_anchors/` as the canonical faction reference for all future generation. **Stage B:** 4 common-card tiles (P1 Cathedral Brother / Self-Scourge / M Pall-Bearer / C1 Bog-Spawn) — tests commons against the locked anchors. Outputs to `art_iterations/_commons_validation/`. Gated on B3.0a smoke test confirming a RunPod pod can run the workflow end-to-end, and Paul confirming ComfyUI install (or running the cloud pipeline) works. Total GPU spend ~25-35 min, ~£0.25.

## Phase 2.10 — Card-treatment economy spec (added 2026-05-02)

Paul locked the Marvel Snap-style cosmetic-treatment system as primary cosmetic monetisation surface. Spec the implementation before B3 art generates so the engine is treatment-aware from day one (retrofitting shader stacks onto a hardcoded CardView is painful).

- [x] T1. Card treatment data model — add `CardTreatment` Resource class + `CardInstance` runtime concept (Card resource + treatment_id + alt_art_id). The Card resource itself stays gameplay-only; cosmetic state is per-instance on the player's collection. Author `treatment_definitions.tres` enumerating the 7 treatment tiers. _(Done 2026-05-09 heartbeat: 5 files — `game/src/data/card_treatment.gd` (Resource), `game/src/data/treatment_catalog.gd` (Resource w/ get_by_id / get_by_tier / get_faction_frame / validate), `game/src/runtime/card_instance.gd` (RefCounted, holds card ref + treatment_id + alt_art_id + acquisition metadata + to_dict for save), `game/data/treatments/treatment_definitions.tres` (12 entries: 1 Default + 5 Faction frames + Foil/Gold/Ink/Prism/Cursed/Ultimate; Ultimate combines [gold, prism]), and TreatmentTier enum added to `enums.gd`. `Card` Resource untouched — anti-P2W invariant preserved (cosmetic state per-instance, never on shared gameplay resource). Sandbox can't run Godot syntax; Paul to verify .tres parses + `catalog.validate()` returns empty in editor. Open Qs for T2 (shader_path empty, layer numerics may shift) and T4 (acquired_via promotion to enum) noted in research_notes.md.)_
- [x] T2. Shader stack design — author the shader pipeline that applies treatments to a base CardView render. Foil = sparkle overlay. Gold = HSV-shift + metallic mask. Prism = animated rainbow gradient. Each treatment is one shader, parameterised. Document the layer order so combos work (Ultimate = Gold base + Prism overlay + animated highlight). _(Done 2026-05-09 19:17 UTC heartbeat: `shader_stack_design.md` authored. Locked: scene-tree layout (pre-allocated slots, no per-card node spawn), 7-tier layer order (Ink=0 / Gold=10 / Frame=15 / Foil=20 / Prism|Cursed=30 / Ultimate-sheen=40 / ChromeUI=100), per-shader uniform + technique + ALU cost spec for all 5 shaders, combo resolution algorithm (combines list expansion + stack_layer sort + slot bind), low-power downgrade rules, perf budget (7× Ultimate worst case ≤ mobile GPU envelope). Shader bodies deliberately deferred to T2.1–T2.9 (need real card-art textures for tuning, which is B3-art-pass dependency). 4 open Qs surfaced for Paul — only Q1 (Frame/Gold layer swap) requires confirmation before T2.7 .tres rebalance; Q2–Q4 are minor edge-case calls.)_
- [x] T3. Faction-frame author — author the 5 free faction-themed base frames per `art_direction.md` §2 (Iron Penitents brass-execution-mask top-arch, etc.). PSD-template approach: master frame template + 5 swappable border-asset packs. _(Done 2026-05-09 23:19 UTC heartbeat: `faction_frames_v0.md` authored — locks master-template approach, 832×1166 canvas, 10-zone slot map, PSD smart-object structure, export spec → `res://game/art/frames/`, all 5 per-faction packs spec'd to motif/corner level (brass arch / shroud-pleat / briar-tangle / foundry-rivet / antler-arch), free-tooling chain (Photopea + Krita), shader-stack integration recap (FrameDecal layer 15). Actual frame PNG authoring deferred to B3.2 frame-pass — T3's contract is "spec exists and can be executed against without re-deriving"; that's met. 4 open Qs for Paul (aspect-ratio rebase, PSD vs flatten, AI-generated frame route, long-name overflow); none block T4. Spec writes against existing T1+T2 contracts — frame_path fields in `treatment_definitions.tres` and FrameDecal slot in `shader_stack_design.md` §3.6 already wired and untouched.)_
- [x] T4. Collection screen UI — placeholder mock for the per-card treatment chooser. Shows owned-treatments per card + locked-treatment paths (gacha / IAP / event). Slot in after B2.10 smoke test ships. _(Done 2026-05-13 heartbeat: `collection_ui_v0.md` authored — 5 ASCII screens (A roster grid, B card detail with live T2 shader stack, C treatment chooser modal with 7-tier grid + live preview + Ultimate combo prereq surfacing, D unlock detail panel as single-source storefront/season-pass/gacha/event-window route, anti-P2W banner on every chooser screen). Display-string conventions locked to match W4. Engine handoff sketched: `GameState.collection: CardCollection` + 3 signals (`treatment_applied / treatment_acquired / collection_filter_changed`), `CardCollection.set_treatment(instance, treatment_id)` API. 3 anti-P2W invariants restated for the implementing engineer (combat never reads treatment_id; set_treatment writes CardInstance not Card; combo ownership computed on-the-fly, never persisted). Pure spec; no .gd / .tres / enum edits this run — real scenes land in B3.x once cards have real art. Storefront wireframe deferred per IMV-1 trim. 5 open Qs for Paul (filter precedence, silhouette loudness, storefront route style, Cursed serial visibility, multi-faction frame UX); none block downstream. Phase 2.10 complete — T1✅ T2✅ T3✅ T4✅.)_

## Phase 2.9 — Mechanical mining from Paul's MTG refs (added 2026-05-02 — see `art_direction.md` §1c)

Paul's MTG references all centre on persist / sacrifice / graveyard-recursion. This is the central fantasy. Map it into Gallowfell mechanics before B2.7 turn engine ossifies.

- [x] M1. **Persist keyword design** — author keyword spec: "When this dies, return it to the lane at end of turn with -1 ATK, can't Persist again this combat." Wire into `GFEnums.Keyword`. Add to faction_bible.md as Ash-Mourners primary mechanic (alongside existing Smoke / Reanimate). Mark 4-6 existing Ash-Mourner cards as candidates for Persist tagging. _(Done 2026-05-10 heartbeat: `keywords/persist_v0.md` authored — end-of-turn return, ATK-1 floor 0, once-per-combat (`CardInstance.has_persisted`), tokens excluded, Resurrect wins over Persist, sacrifice synergy preserved, Hanging Hour M4-hook reserved. `GFEnums.Keyword.PERSIST` added as 15th keyword. `faction_bible.md` Ash-Mourners §2 names Persist as primary keyword. `cards_ash_mourners_v1.md` appended "Persist candidates" §: M5 Last Censer-Bearer / M12 Necrologist / M20 Bone-Shroud Acolyte / M22 Hollow Mortician / M24 Choir of the Long Dead — design suggestion only, no `.tres` edits this pass; tagging happens at M2. Anti-P2W invariant restated. 3 open Qs for Paul deferred (ATK-floor, enemy-side Persist, UI display) — none block M2.)_
- [x] M2. **Sacrifice-and-return loop hardening** — review Iron Penitents (self-damage = soft-sacrifice) + Coven (explicit sacrifice combos). Identify 2-3 missing combo enablers (sacrifice outlet, return-to-hand effect, sacrifice-payoff). Author them. _(Done 2026-05-10 heartbeat: 3 cards authored — P41 Last Vows (0c C SPELL, sac outlet, `keywords=[SACRIFICE,BLEED]`), C41 Bog-Bargain Recall (2c U SPELL, return-to-hand with explicit Persist-marker reset), C42 Black Mire Pact (2c C TRAP lane-wide, sacrifice payoff, 3 charges). Markdown appended to `cards_iron_penitents_v1.md` + `cards_coven_v1.md`; `.tres` files written under `game/data/cards/iron_penitents/P41.tres`, `game/data/cards/coven/C41.tres`, `game/data/cards/coven/C42.tres`. Engine flag for B2.7+: C42 is the first lane-wide passive trap — needs a `LaneEffect` Resource separate from per-tile `Trap` instances. 3 open Qs for Paul logged in research_notes.md (Recall+Persist marker timing, charge-counter UI, Last-Vows-on-Persisted body double-Persist) — none block M3.)_
- [x] M3. **Graveyard-as-resource Warlord** — design 1 new Warlord (or repurpose an existing one's Tier-3 alt-fire) where the discard pile counts as a usable resource. E.g. "Glean: pay 1 mana + 2 cards from discard to summon a copy of any unit in discard at -1 ATK." Channels Deathrite Shaman energy. _(Done 2026-05-10 14:18 UTC heartbeat: repurposed Court-Necromant Sieren's T3 alt-fire (faction-canonical fit; roster locked at 11 so no new Warlord). `sieren_t3_glean_v0.md` authored — Glean = 3 mana + exhaust 2 discard cards + consume target unit's discard entry → summon copy of any UNIT in discard to friendly tile at -1 ATK (floor 0). Tokens excluded; already-Persisted bodies excluded (perpetual-recur block). Pattern-compliant per `warlord_tiers_v0.md` §3.3 (same cost, same cooldown band, opposite-axis effect: lane-control DoT → resource conversion). Persist (M1) + sacrifice-loop (M2) hooks land cleanly. W1's illustrative "Pall Writ" alt-fire for Sieren flagged for reassignment to Saint of Gallowsmoke (W8) — better thematic fit. 5 open Qs for Paul logged in spec §6 (none block W2). Glean is a v0 spec for IMV-2 build per internal MVP scope; engine wiring deferred to W5.)_
- [x] M4. **Hanging Hour × Persist interaction** — at the Hanging Hour, all units that died this combat Persist back at full stats (not -1). Boss-escalation made even more terrifying. Reflects "the curse keeps the dead from staying dead, the Hanging Hour intensifies it." _(Done 2026-05-10 heartbeat: `keywords/hanging_hour_persist_v0.md` authored — locks trigger turn (5 standard / 4 boss), corpse-queue dedup-latest-death rule, PERSIST + non-token filter, full-stats restoration with one-shot bypass of `has_persisted` lock, friendly+enemy symmetry (boss-encounter escalator), W11 Saint × Speak-the-Name ordering (Persist drain runs BEFORE freeze takes effect, otherwise W11's lore-payoff turn bricks), anti-P2W invariant restated (Cursed treatment is visual-only; engine MUST NOT branch combat on treatment_id). Engine wiring sketch'd — adds `hanging_hour_turn` + `hanging_hour_fired` + `corpses` + `hanging_hour_struck` signal to Combat.gd + `TurnEngine.drain_hanging_hour_persists` static method. Pure spec; no enum / .tres / .gd edits this run. 4 open Qs for Paul logged in spec §"Open questions" — none block any subsequent backlog item. Phase 2.9 mechanical-mining pass now complete.)_

## Phase 2.7 — Warlord tier system (added 2026-04-30 by Paul)

Each Warlord earns XP on wins-with-that-Warlord. Tiers unlock sideways powers, never bigger numbers.

- [x] W1. Tier system design — XP curve (Tier 2 ≈ 10-15 wins, Tier 3 ≈ 30 wins, Tier 4 ≈ 60-80 wins), variant-passive choice mechanic (1 of 2 at Tier 2), signature alt-fire pattern (Tier 3), mastery reward template (Tier 4). Anti-pay-to-win bound: NEVER unlocks raw power. Output: `warlord_tiers_v0.md`. _(Done 2026-05-02 14:17 UTC heartbeat: `warlord_tiers_v0.md` authored. XP curve hits all 3 target bands (12/32/72 A0 wins). Tier 2 = 1-of-2 variant passive (default stays available, both sidegrades). Tier 3 = signature alt-fire (same cost, opposite-axis effect). Tier 4 = mastery skin + lore reveal + title + Warlord-specific Ascension slot. Boosters cap ×3.0 stacked → whale floor ~25 wins, free floor ~72 wins to T4. Engine handoff sketched for W5. 6 open questions surfaced — see doc §7.)_
- [x] W2. Tier content per Warlord — for all 10, author Tier 2 variant passives (×2 each), Tier 3 signature alt-fires, Tier 4 mastery cosmetic + lore-reveal one-liner + Ascension-modifier slot. Output: `warlord_tiers_full.md`. _(Done 2026-05-11 heartbeat: `warlord_tiers_full.md` authored — full content for all 11 Warlords (free 5 + paid 5 + lore-locked W11). Each entry has Default re-stated + T2 Variant A (offence-leaning sidegrade) + T2 Variant B (defence/utility sidegrade) + T3 alt-fire (same cost, opposite axis) + T4 mastery (skin name + visual cue, ≤30-word lore reveal, mastery title, Ascension modifier). Sieren T3 = Glean per M3; Pall Writ reassigned to Saint of Gallowsmoke (W8) T3. W10 + W11 use campaign-tier Ascension slots per W1 Open Q4 lean. Anti-P2W audit re-applied at the bottom. IMV-1 trim recommendation drafted (Vyrrun/Sieren/Mhar T1+T2 only; T3 .tres authored-but-locked; T4 deferred to IMV-2 after B3.x cosmetic pipeline lands). 6 open Qs for Paul logged in spec — none block W3 (XP-booster economy integration) or W4 (UI mock). Pure design doc; no .gd / .tres / enum edits this run — engine wiring is W5's job.)_
- [x] W3. XP economy integration — wire battle-pass and daily-quest XP boosters into the tier system. Reflect in `monetisation_map.md`. _(Done 2026-05-11 heartbeat: pure integration pass — booster mechanics already locked in W1. `monetisation_map.md` edits: §2 starter bundle clarified (multiplier not flat XP), §6 BP added free-track ×1.10 from lvl 25 + premium ×1.25 season-long + engine-handoff line, §11 expanded to "Daily reward calendar + daily quests" with 3-quest spec (one-shot ×1.5 on win-with-Warlord-X), new §13 master XP-booster registry table (6 sources × multiplier × duration × stack × MVP × x-ref) + worked F2P/payer/whale stack examples (whale ×5.91 raw clamps to ×3.0, ceiling holds at ~25 wins to T4) + engine handoff (Dictionary registry, computed-not-cached, one-shot self-unregister + `xp_multiplier_consumed` signal), anti-P2W guardrails added rule 6 (multiplier-only / no-flat / no-skip / ×3.0 server-enforced), MVP table row added. No .gd / .tres edits — engine wiring is W5's job. 3 open Qs for Paul (skin+quest stacking surface, free-track threshold lvl 25 vs 30, multiplier display string `+25%` vs `×1.25`); none block W4 or W5.)_
- [x] W4. UI mock — Warlord-select screen with tier ladder visible (locked / unlocked / next-XP). ASCII or markdown wireframe v0. _(Done 2026-05-11 heartbeat: `warlord_select_ui_v0.md` authored — 6 screens (A roster grid, B detail panel with tier ladder, C T2 variant picker, D T3 alt-fire picker, E T4 mastery one-shot, F XP-booster detail drawer), engine handoff signal list for W5 (`warlord_xp_gained` / `warlord_tier_changed` / `xp_multiplier_changed` / `xp_multiplier_consumed`), full anti-P2W audit at the UI surface layer, display-string locked to `+X% XP` additive-percentage with `×N` notation only in a "math" explainer footer (closes W1 Q5 + W3 §13 Q3 pending Paul-sign-off), cap-clamp visualisation answers W3 §13 Q1. 5 open Qs for Paul (variant picker radio-vs-fullscreen, mastery reveal pacing, booster-strip HUD persistence, W10 silhouette-lock-or-not, display-string sign-off). None block W5.)_
- [x] W5. Engine wiring — extend `Warlord` Resource class with `tier_unlocks: Array[Resource]` field; add XP store to `GameState`. Order this after Phase 2.6 C1 lands. _(Done 2026-05-11 heartbeat: Phase 2.7 closeout. On entry, all the heavy lifting was already in place from earlier heartbeats — `warlord.gd` has `tier_unlocks: Array[Resource]` + `get_tier_content` + `validate`; `tier_content.gd` carries full T1–T4 shape + tier-relative `validate`; `game_state.gd` has `TIER_THRESHOLDS = [0,1800,4800,10800]`, `XP_MULTIPLIER_CAP = 3.0`, `warlord_xp` + `xp_multiplier_sources` + `_xp_pending_consume`, 4 signals (warlord_xp_gained / warlord_tier_changed / xp_multiplier_changed / xp_multiplier_consumed), and the full `gain_warlord_xp` / set/clear/mark_one_shot / `get_effective_xp_multiplier` / `get_warlord_tier` / `get_warlord_xp` / `get_xp_to_next_tier` / `_tier_for_xp` / `_drain_pending_consumes` API with anti-P2W guards (rejects zero/negative/empty-id; multiplier clamped server-side; no public set_tier). `warlord_test.gd` already authored with 30+ assertions covering all four tier-boundary crosses, empty/single/stacked/clamped/cleared multiplier flows, one-shot consume flow, tier-cross signal emission (single fire on cross + skip-tier T1→T4 emits one signal with final-tier payload), xp_to_next_tier at all bands, anti-P2W rejections, and TierContent + Warlord `validate()` shape checks. Gap closed this run: the test was NOT wired into the dev-test runner — added one line to `game/src/main.gd` so `RUN_DEV_TESTS=true` exercises the suite. Per-Warlord .tres authoring deliberately NOT in scope here (IMV-1 trim per `internal_mvp_scope.md`) — that's a separate authoring batch pulling from `warlord_tiers_full.md`. Sandbox can't run Godot syntax; Paul to confirm `[warlord_test] PASS` line appears in editor console on next launch. Phase 2.7 now COMPLETE — W1✅ W2✅ W3✅ W4✅ W5✅.)_

## Phase 2.8 — Lore consistency reconciliation (flagged 2026-04-30)

Faction names have drifted across docs. Two parallel Gallowfell namings co-exist:
- **Track A** (faction_bible.md, warlords_v0.md, lore_gallowfell.md v0.1, GDD v0): Iron Penitents / Ash-Mourners / Coven of the Black Mire / The Last Legion / Skinward Pact.
- **Track B** (cards_v0.md v1.0, warlords_v1.md, the original lore_gallowfell.md draft): Iron Penitents / Withered Court / Hollow Pact / Ferrum Host / Sable Wilds.

- [x] L1. **Canonical names locked by Paul 2026-04-30: Track A.** Iron Penitents / Ash-Mourners / Coven of the Black Mire / The Last Legion / Skinward Pact.
- [x] L2. Heartbeat: globally rename Track-B leftovers across `cards_v0.md`, `warlords_v1.md`, any `.tres` files, and any other doc still using Withered Court / Hollow Pact / Ferrum Host / Sable Wilds → replace with the Track-A equivalents (Ash-Mourners / Coven of the Black Mire / The Last Legion / Skinward Pact). Iron Penitents stays. _(Done 2026-05-01: batch-renamed across `cards_v0.md` / `gdd_v0.md` / `faction_bible.md` / `warlords_v1.md` / `lore_gallowfell.md` / `C1.tres` via sed — 38 references updated + 1 double-article fix ("the The Last Legion" → "the Last Legion"). Engine constants (`GFEnums.Faction.WITHERED_COURT/HOLLOW_PACT/FERRUM_HOST/SABLE_WILDS` in `enums.gd`) preserved — internal-only. Remaining Track-B mentions in `backlog.md` and `research_notes.md` are historical decision-narration and intentionally retained.)_
- [x] L3. Re-export the pitch .docx after rename so the public-facing brief matches. _(Done 2026-05-11 14:25 UTC heartbeat: audit-only, no re-export needed. Unzipped `The Curse of Gallowfell - Pitch.docx` and grep'd `word/document.xml` for both naming tracks — Track-B references = 0, Track-A references = 18 across faction list / chapter-unlock paragraph / sample-cards block / curse-mechanics paragraph. Docx mtime 2026-04-30 18:28 UTC predates L2's `.md` sed pass — whoever exported it in the 2026-04-30 live session already used the locked Track-A names. Build script `build_pitch_doc.js` lives in an archived prior-session outputs folder unreachable from this sandbox, but no re-export is needed. If a future doc edit re-introduces faction churn, archive `build_pitch_doc.js` into the project folder first so the next re-export isn't bottlenecked on a vanished session. See research_notes.md "L3 — Pitch .docx Track-A audit" for full audit.)_

## Phase 2.12 — Synergy mining + new keyword pass (added 2026-05-15 by Controller)

Second-pass design refinement after Phase 2.6 (deck depth) and Phase 2.9 (mechanical mining from MTG refs) are complete. Plug remaining mechanical gaps and shore up cross-faction balance before B3 art generation freezes card-side iteration. Heartbeat-sized per item, same pattern as Phase 2.6 and 2.9.

- [x] M5. **TAUNT keyword design v0.** Spec `keywords/taunt_v0.md` — mechanics, range scoping, anti-token rule, interactions with PIERCE/SHIELD/FEAR/SMOKE/PERSIST/ROOT/Hanging-Hour/Cleave, faction allocation (Last Legion primary, Iron Penitents secondary), engine wiring sketch, anti-P2W invariant, 4 open Qs (none block E1). Adds 16th GFEnums.Keyword.TAUNT. Updates faction_bible.md §4. Markdown-flags 4 Legion + 2 Penitents cards for E1 tagging. _(Done 2026-05-15 09:55 UK heartbeat (Controller). `.tres` keyword-array edits deliberately deferred to E1 — same M1 → M2 pattern as PERSIST.)_
- [x] E1. **TAUNT engine wiring + .tres tagging.** Apply Keyword.TAUNT to the 6 markdown-flagged cards (L7, L11, L18, L33, P3, P34) by editing each card's `.tres` `keywords` array. Extend `TurnEngine.choose_enemy_target` (or equivalent target-selection function) with the in-lane TAUNT filter per spec §"Engine wiring sketch". Add 3 new assertions to `turn_engine_test.gd` covering MELEE-in-range-redirect, MELEE-out-of-range-falls-through, Cleave-ignores-TAUNT. Wire into `RUN_DEV_TESTS=true` flow. Paul confirms `[turn_engine_test] PASS` in editor. _(Done 2026-05-26 by Cowork Claude. Engine: extended `_friendly_on_tile` in `game/src/runtime/turn_engine.gd` with a TAUNT-preference branch that overrides the cheapest-cost rule when a non-token TAUNT body shares the enemy's collision tile; ranged-enemy-scoping noted as future when ranged enemy attacks land. Test: `_scene_d_taunt_targeting` appended to `game/src/runtime/turn_engine_test.gd` with D1 (TAUNT redirects on shared tile), D2 (out-of-tile TAUNT falls through to cheapest), D3 (friendly attacks unaffected — Cleave-ignores-TAUNT documentation). `.tres` tags: TAUNT (15) added to L7, L18, L33, P3, P34 keyword arrays. **Doc/data drift caught:** L11 is `card_type=1` (SPELL "Form Ranks") in the live `.tres`, not the markdown's UNIT "Iron Watch Standard-Bearer" — SKIPPED to avoid putting a UNIT-only keyword on a spell; flagged for Controller to reconcile the spec doc or rename the card. Also flagged: P3 lives at `game/data/cards/P3.tres` (not in the `iron_penitents/` subfolder like P34), causing a cosmetic `res://assets/cards/cards/P3.webp` double-`cards/` art_path — works, but ugly. Sandbox cannot run Godot; Paul confirms `[turn_engine_test] PASS` next time the editor is open.)_
- [x] M6. **Cross-faction synergy refinement v0.2.** Re-audit the anti-synergy grid in `archetypes_v0.md` after TAUNT. With TAUNT in Legion's toolkit, three of the existing counter relationships shift (Bleed-Stack vs Banner-Buff hardens further; Coven Bog-Spawn loses tempo vs Legion's soak line; Skinward Big-Monster's lane-pressure breaks against a TAUNT-leader). Re-state the 5x5 grid with v0.2 deltas and a brief impact paragraph each. Output: append `## v0.2 (post-TAUNT)` section to `archetypes_v0.md`. _(Done 2026-05-26 by Controller: `## v0.2 (post-TAUNT) — cross-faction synergy refinement (M6, 2026-05-26)` section now landed in `archetypes_v0.md` lines 289+. **Approach taken: magnitude shifts, not edge swaps.** Existing v0.1 structural property (every archetype deals 1 + receives 1) PRESERVED. 5 deltas: 2 hardened (L-Banner vs P-Bleed intensified via TAUNT+SHIELD double-soak; L-Rally vs S-Beast intensified via L11 TAUNT pulling Wolf attacks onto SHIELD-1 body), 2 softened (L-Rally vs M-Smoke-Fear from full to partial counter; L-Echo vs S-Big-Monster softens slightly via L18 TAUNT+ECHO self-replay), 1 marginal (P3 Cathedral Brother enables Penitents hybrid TAUNT-front + Cleave-back build). 4 emergent design flags raised for C7 playtest sim: L18 Echo-Sergeant TAUNT+SHIELD+ECHO triple-stack balance review (cap ECHO procs per turn); P3 is a genuine Bleed-Stack power buff; Banner-Buff's anti-Bleed becomes load-bearing; Mourners potentially under-pressuring Legion overall. NB: a prior Controller heartbeat had marked M6 done with an 'edge-swap' approach but no corresponding section ever shipped — that note was orphaned, this run is the authoritative completion. No card adds, no .tres edits. Markdown-flag tags queued for P3 (Penitents) + L7/L11/L18/L33 (Legion) — to be applied at E1 engine-wiring heartbeat alongside TAUNT keyword wire-in.)_
- [x] M7. **Faction-internal sub-archetype cohesion check.** For each of the 15 sub-archetypes (5 factions × 3 sub-archetypes), audit that the identity card (the 4-cost rare that names the strategy), the payoff card, and the 2/3/4-cost spine all share at least 2 connecting keyword/mechanic references. Flag any sub-archetype where the spine is too "flex" to deliver a recognisable strategy by turn 4. Output: append `## Sub-archetype cohesion audit` section to `archetypes_v0.md` with per-archetype ratings (tight / loose / re-spec needed) + named cards to redesign. _(Done 2026-05-26 by Controller: 12 TIGHT, 3 LOOSE (B2 Resurrect Spam, D3 Banner-Buff, E3 Beast-Summon), 0 re-spec. Recommendations queue 4 new commons — Wraith-Caller of the Dirge (3c U, M-faction), Banner-Bearer of the Crowned Anvil (3c U, L-faction), Pack-Caller Initiate (3c U, W-faction), Den-Mother of the Cinderwood (4c U, W-faction) — appended to archetypes_v0.md §Sub-archetype cohesion audit. Card count 203 → 207. No art-pipeline impact: commons ride Stage-B validation, not Stage-A anchors.)_
- [x] M8. **Event-card content v0 — 10 prompts.** B2.9 added the EVENT NodeKind but no event-card content exists. Author 10 narrative-choice events spread across the 5 biomes (Cathedral Ruins / Ash Quarter / Black Mire / Forgotten Parade / Cinderwood / Gallows Hill boss biome). Each event: title, 2-line setup prose, 3-choice picker, mechanical payoff for each choice (gold / heal / curse / relic / card-add / card-remove), faction-flavour binding. Output: `events_v0.md`. Engine wiring is M8.E1 (separate item). _(Done 2026-05-18 by Controller: 10 narrative-choice events authored in `events_v0.md` — across 5 biomes (Cathedral Ruins / Ash Quarter / Black Mire / Forgotten Parade / Cinderwood) + Gallows Hill boss biome. Each event has title, 2-line second-person setup, 3 choices with mechanical payoff (gold / HP / relic / card-add / card-remove / curse) and faction-flavour binding. Lists 7 new relic identifiers + 8 new curse identifiers for stub definition in card.gd / relic.gd. Engine wiring is M8.E1, queued separately. Art prompts for each event archived in `art_specs/events_art_v0.md` as future M9 deliverable when SDXL pipeline clears B3.0a.)_
- [x] M9. **Shrine + Rest content v0.** Same pattern for SHRINE (one-time blessing/curse) and REST (heal-or-upgrade) nodes. 4 shrine variants + 2 rest variants. Output: `shrines_v0.md` + `rests_v0.md`. _(Done 2026-05-19 by Controller. `shrines_v0.md` authored — 4 shrines (S1 Gallows / Penitents, S2 Bog / Coven, S3 Ash / Ash-Mourners, S4 Hollow / Legion+Skinward crossover) with Pray vs Defile two-outcome design. `rests_v0.md` authored — 2 rests (R1 Mourning Hearth standard, R2 Standing Stone rare / Skinward flavour) with Heal vs Upgrade. Per-instance upgrade rules on CardInstance.upgrade_count + primary-stat-delta dispatch. Distribution rules (faction-weighted shrines, rare-rest 4× ratio) and graph constraint (no-adjacent-rests) authored. Engine wiring sketched for M9.E1 — new Shrine + Rest Resource classes, 8 ShrineEffect kinds, GameState lifecycle signals. Anti-P2W invariant restated. 3+3 open Qs for Paul logged; none block M10.)_
- [x] M10. **Hanging Hour boss design v0 — Chapter 1.** Per `keywords/hanging_hour_persist_v0.md`, the Hanging Hour fires at turn 5 (standard) or turn 4 (boss) and overrides Persist's once-per-combat lock. Author the Chapter 1 boss encounter: name, signature mechanic, wave-spawn pattern across pre-/at-/post-Hanging-Hour turns, reward roll on victory. One boss for now; chapters 2-3 deferred to IMV-2. Output: `bosses_v0.md`. _(Done 2026-05-19 by Controller. `bosses_v0.md` authored — Chapter 1 boss is **The Black-Bell Choir** (Ash-Mourners, Gallows Hill boiome). Signature: Toll the Bell — at Hanging Hour (turn 4), all enemies dead this combat resurrect at full HP+ATK with PERSIST keyword inherited from boss. Stat block: 60 HP / 4 ATK / LONG range. Tri-phase wave schedule pre-/at-/post-HH. Per-faction interaction notes for all 5 player factions. Reward: guaranteed Chapter-1 relic + 3-card pick + 10 gems. Defeat: 5-gem retry path per balance doc. Engine wiring sketched for M10.E1 — `Enemy.is_boss + signature_ability_id` fields, `BossAbilities.dispatch()` static module hooked on `hanging_hour_struck` signal, no new wave type needed. Anti-P2W invariant restated. 4 open Qs for Paul logged. Tuning fallback levers documented (80%-resurrect-HP, revert-HH-to-turn-5) if Toll feels punishing in playtest.)_

## Phase 2.13 — LOOSE-archetype spine cards (added 2026-05-26 by Controller, follow-on from M7)

Per `archetypes_v0.md` §Sub-archetype cohesion audit, M7 flagged 3 LOOSE sub-archetypes (B2 Resurrect-Spam, D3 Banner-Buff, E3 Beast-Summon) and queued 4 new commons to harden each spine. These cards slot into the existing faction pools as NET-NEW entries (no redesigns of authored cards). Total card count rises 203 → 207. Pool sizes tick from 40 → 41 (Ash-Mourners), 40 → 41 (Last Legion), 40 → 42 (Skinward Pact); accept the deviation from the 40-card target — flagged at C7-v0.2 next balance pass.

Each item: author markdown entry in the appropriate `cards_<faction>_v1.md` (under the LOOSE archetype's UNIT block), author the matching `.tres` under `game/data/cards/<faction>/<id>.tres`, leave art spec for the next sweep (commons ride Stage-B common-tile validation per `pipeline_spec.md` §5 — no new anchors needed).

- [x] N1. **M41 Wraith-Caller of the Dirge** (3c U, Ash-Mourners, B2 Resurrect-Spam spine). "When a friendly Mourner dies, the next Mourner you play this turn costs 1 less." 1-per-turn cap mirrors M12 Necrologist's once-per-turn lock. Output: append to `cards_ash_mourners_v1.md` Resurrect-Spam UNIT block + author `game/data/cards/ash_mourners/M41.tres`. _(Done 2026-05-26 11:00 UK by Controller. Card authored: 3c U / 3 HP / 2 ATK / MELEE / CD-1 / keywords=[] / cost-trigger spelled in effect_text (no formal Mourner-death-trigger keyword exists — engine reads card text and dispatches via Combat's existing `friendly_unit_died` signal path, same as M22 Hollow Mortician's ATK buff). Naming collision with M21 "Wraith-Caller" noted in flavour block — the longer "of the Dirge" suffix is per M7's prescription and the cards are mechanically distinct (M21 = on-play SUMMON; M41 = on-friendly-death cost-reduction). Mechanical overlap with M20 Bone-Shroud Acolyte addressed in spec: M20 fires only on its OWN death, M41 fires on ANY friendly Mourner death (broader trigger surface justifies the 3c cost vs M20's 2c). `M41.tres` written; faction=1, card_type=0, rarity=1, cost=3, hp=3, attack=2, attack_range=1, cooldown=1, keywords=Array[int]([]), draftable=true, starter=false, unlock_tag=&"". cards_ash_mourners_v1.md Distribution + Rarity-skew + Pool-totals + Per-archetype-density tables updated for new 41-card pool (Units 25, Uncommon 13, Resurrect-Spam primary 14). Art-spec deferred per Phase 2.13 preamble — commons ride Stage-B validation, no new anchor required. 3 open Qs for Paul logged in cards_ash_mourners_v1.md change log (cap-per-turn vs cap-per-combat, stacking with M20, "next Mourner" scope = hand-only or includes summon-from-effect)._
- [x] N2. **L41 Banner-Bearer of the Crowned Anvil** (3c U, Last Legion, D3 Banner-Buff spine). "While a friendly banner stands in this row, gains +1 ATK and Pierce." Anchors the 1c→3c→4c R→5c R Banner rarity ramp. Output: append to `cards_last_legion_v1.md` Banner-Buff UNIT block + author `game/data/cards/last_legion/L41.tres`. _(Done 2026-05-26 13:18 UTC by Controller. Card authored: 3c U / 3 HP / 2 ATK / SHORT range / CD-1 / `keywords = Array[int]([])` — PIERCE is conditional via effect_text only, same convention as M41 (cost-trigger spelled in text, not enum). Range SHORT for parity with the 3c Banner-Buff slot (L30 Crown-Anvil Veteran, L31 Iron Choir-Standard) and the polearm chassis. Found a prior partial draft of L41.tres on disk (keywords=[1] PIERCE always-on + MELEE range) — reconciled to the correct conditional-PIERCE + SHORT convention this run. cards_last_legion_v1.md updated for 41-card pool: Distribution (Units 24→25, total 40→41), Rarity-skew (Uncommon 12→13), Pool-totals (Total 40→41, Uncommon 12→13, Units 24→25), Per-archetype-density (D3 Banner-Buff 13→14), Cost-curve (3-cost 10→11). v1.1 change-log section appended with 3 open Qs for Paul (banner-scope = does L34 Crowned Anvil Standard count; Pierce scope = base or bonus-only; multi-banner stacking = single or stacked) — none block N3 / N4. Art-spec deferred per Phase 2.13 preamble — commons ride Stage-B validation, no new anchor required.)_
- [x] N3. **W41 Pack-Caller Initiate** (3c U, Skinward Pact, E3 Beast-Summon spine). "When you summon a Wolf-Token in your lane, draw 1 card. Cap once per turn." Gives E3 a card-draw engine that doesn't read on E1's solo-big-body chassis. Output: append to `cards_skinward_pact_v1.md` Beast-Summon UNIT block + author `game/data/cards/skinward_pact/W41.tres`. _(Done 2026-05-26 10:34 UK by Controller. Card authored: 3c U / 2 HP / 2 ATK / Range-S (attack_range=2) / CD-1 / `keywords = Array[int]([])` — Wolf-Token-summoned trigger spelled in effect_text (engine reads card text and dispatches via the existing token-spawn signal path used by W31 Pelt-Bound Shaman and Pack-Call of the Cinderwood). Cap-once-per-turn convention matches M41 / M12 Necrologist lock. Faction-id 4 / card-type UNIT. `W41.tres` written to `game/data/cards/skinward_pact/`. cards_skinward_pact_v1.md Beast-Summon UNIT block updated (W41 + W42 entries at lines 202–207); change-log notes pool growth 40 → 42 (Units 60% target preserved at 25/41 rounded). Art-spec deferred per Phase 2.13 preamble — commons ride Stage-B common-tile validation, no new anchor required. Open Qs for Paul logged in spec change-log (does "your lane" mean caster's row or summoned-token's row when Den-Mother is in a different row; do Cub-Tokens trigger W41 or only Wolf-Tokens specifically). Tick reconciled 2026-05-26 evening — files landed earlier in day but backlog state lagged, classic phantom-untick caught by verify-against-actual-files rule.)_
- [x] N4. **W42 Den-Mother of the Cinderwood** (4c U, Skinward Pact, E3 Beast-Summon spine). "Your Wolf-Tokens gain +1/+1 and Lifesteal while she is in lane." Curve-topping common slot for the E3 spine. Output: append to `cards_skinward_pact_v1.md` Beast-Summon UNIT block + author `game/data/cards/skinward_pact/W42.tres`. _(Done 2026-05-26 10:34 UK by Controller. Card authored: 4c U / 4 HP / 2 ATK / MELEE (attack_range=1) / CD-1 / `keywords = Array[int]([])` — Lifesteal granted via effect_text only, same text-only Lifesteal convention as W3 Cinderwood Stalker (third Lifesteal-text-only card → flagged for `LIFESTEAL = 16` enum promotion at next balance pass per spec doc Open-Q6 already on file). +1/+1 aura on Wolf-Tokens via effect text; mirrors W34 Den-Mother's token-summon pattern (deliberate name echo — W42 is the "of the Cinderwood" variant on the W34 chassis). Faction-id 4 / card-type UNIT. `W42.tres` written. cards_skinward_pact_v1.md updated alongside W41 (single combined Phase 2.13 entry block). Pool grows 40 → 42 (N3 + N4 both Skinward Pact); accept deviation from 40-card target per Phase 2.13 preamble — flagged for C7-v0.2 balance pass. Art-spec deferred. No art anchor regen — commons. Open Qs for Paul: stacking with W34 Den-Mother (does aura double-apply for tokens in lane while both Den-Mothers are in lane, or single-binding?); Lifesteal interaction with token short-circuit death (Wolf at 1 HP eats a 2-damage hit — does Lifesteal heal for 1 or 2 before death resolves?). Tick reconciled 2026-05-26 evening — phantom-untick caught.)_

**Phase 2.13 — DONE 2026-05-26 (Controller).** All 4 LOOSE-archetype spine commons authored: M41 Wraith-Caller of the Dirge (B2), L41 Banner-Bearer of the Crowned Anvil (D3), W41 Pack-Caller Initiate (E3), W42 Den-Mother of the Cinderwood (E3). Net card count 203 → 207 as designed. Pool deviations from 40-card target: Ash-Mourners 41, Last Legion 41, Skinward Pact 42 — accepted per phase preamble, queued for C7-v0.2 balance pass. **M42 Wraith-Caller duplicate reconciled** — an orphan .tres + spec block under cards_ash_mourners_v1.md (line 269) was authored in a prior Controller pass that wasn't aware of M41 being queued for N1; both files name the same card with different stats. M41 is canonical per N1 backlog; M42.tres and its spec block removed this run. See cards_ash_mourners_v1.md v1.1 change-log addendum.

## Phase 2.14 — Post-Phase-2.13 balance audit (queued by Controller hourly cycle, 2026-05-27)

Phase 2.13 commons + M5 TAUNT keyword + Phase 2.12 M6 v0.2 grid all landed without a follow-up cross-faction balance audit. The "queued for C7-v0.2 balance pass" pointer in Phase 2.13 preamble + M6 v0.2 emergent-design-flags + the four named risks (L18 ECHO triple-stack, P3 power buff, Banner-Buff load-bearing, Mourners under-pressuring) all need a single consolidated disposition before M8/M9/M10 narrative content + B-series engine work proceeds against a known balance frame.

- [x] C7-v0.2. **Cross-faction balance audit (post-TAUNT + Phase 2.13 pool growth).** Re-run C7's 4-axis check (rarity skew / cost curve / counter symmetry / archetype viability) over the 207-card pool with v0.2 grid + M5/E1 TAUNT keyword wiring. Disposition the 4 emergent flags from M6 v0.2. Recommend Option α/β/γ for symmetry restoration. Output: `c7_v0_2_balance_audit.md`. _(Done 2026-05-27 00:25Z by Controller hourly backlog cycle #8. 8 numbered sections + 7 open Qs for Paul + 8 recommended actions (R1–R8). Pool integrity ACCEPT (40-45/faction recommended as new canonical target); rarity skew ACCEPT (Coven/Skinward +1 Uncommon within tolerance); cost-curve drift ACCEPT (3 net 3-cost adds all spine-cards by design). Symmetry recommendation: **Option β** (E E3 Beast-Summon → L D2 Tempo-Echo new counter) — restores E3 to 1 deal, brings Legion to 2 deals via D1, materially closer to symmetric than v0.2 raw. v0.2.1 grid table appended with single edge change (D2 counter swap from Coven-Poison to Skinward-Beast-Summon). 4 emergent flags dispositioned: F1 ECHO triple-stack → cap 1/turn at E1 engine wiring; F2 P3 power buff → ACCEPT no card edit; F3 Banner-Buff load-bearing → watch-and-measure post-playtest; F4 Smoke-Fear softens → TAUNT-conditional Fear-Caller -2 ATK (M14 effect_text refresh, restores Mourners' deal count). L D1 Rally pre-emptive nerf (Forge-Conscript 3→4) recommendation: HOLD in pocket — wait for playtest data. C8 playtest sim charter named as natural follow-on. No card adds, one potential .tres edit (M14 conditional on Q4 accept), no art-pipeline impact, M8/M9/M10 unblocked. Master file 02_Gallowfell.md updated.)_

## Phase 2.15 — Playtest sim charter (queued by Controller hourly cycle, 2026-05-27)

C7-v0.2 raised 6 numeric judgment calls + 7 open Qs that need empirical grounding before any further .tres / engine tuning. A 30-game pairwise sim across 15 archetypes (3,150 games / ~5 min wall-clock on a Python mirror) gives the next tuning pass real numbers instead of design intuition. Authoring the **charter** is heartbeat-actionable now; **execution** is gated on Paul Q1 (Option β endorsement) + B2 build playable in editor (already met).

- [x] C8. **Playtest sim charter authored.** `c8_playtest_sim_charter.md` — 8 sections covering purpose / methodology (15 archetypes × 30 games = 3,150-game grid, Route A Python mirror first, Route B Godot headless deferred) / pass-fail criteria mapped to C7-v0.2 Q1–Q5 + Q7 / out-of-scope list / 5 deliverables D1–D5 with gating / 7 open Qs (this charter's own). Charter recommends Route A (Python mirror) for v0 cheaply, AI policy module spec'd as D2 (heartbeat-authorable next cycle, grid-agnostic), sim runner D3 gated on Paul Q1 endorsement. Out-of-scope: player-skill modelling, Hanging Hour boss-specific mechanics, map-node economy, treatment cosmetics, Warlord tiers. Open Qs lean toward staying-cheap-at-v0 (n=30, mirror-policy, single mulligan rule). _(Done 2026-05-27 01:20Z by Controller hourly backlog cycle #9. No .tres edits, no engine wiring, no card adds — pure spec. D2 AI policy doc heartbeat-authorable next cycle without Paul Q1 disposition. D3 sim runner gated on Q1. Phase 2.15 charter step closed; D2 next.)_
- [x] C8.D2. **AI policy doc** — `c8_ai_policy_v0.md`. Spec the per-turn AI heuristic policy per charter §2.6 (5 numbered rules — play priority, lane choice, spell timing, resource trade-offs, end-of-turn pass). Grid-agnostic; can author without Paul Q1 disposition. Heartbeat-actionable. _(Done 2026-05-27 02:25Z by Controller hourly backlog cycle #10. `c8_ai_policy_v0.md` authored — 10 sections covering scope / inputs / outputs / 5-rule decision algorithm (Rule 1 cost-descending spine-weighted play priority with 3-bucket partition + mana-waste cap ≥3; Rule 2 SPREAD/STACK/THREAT_RESPONSE lane-bias matrix per all 15 archetypes; Rule 3 4-bucket spell timing — trap turn-1, removal at 3-tile mark with lethal-failsafe override, buff immediate-on-friendly, utility immediate; Rule 4 sac-fodder ordering with PERSIST-aware exclusion + Hanging-Hour pre-positioning + Glean conditional forward-compat; Rule 5 END_TURN tri-condition with hand-prune) / per-archetype spine+payoff lookup table (15 archetypes incl. Phase 2.13 M41/L41/W41/W42 + M2 P41/C41/C42 adds) / anti-P2W invariants (5 forbidden state reads, MUST-NOT-READ list) / determinism + reproducibility / D3 engine-truth verification gates (parse archetypes_v0.md at runtime, mirror-vs-engine drift detection, no hard-coded archetype data) / 8 open Qs for Paul (identity-overlap deck construction, D2 self-payoff loop, Token-fodder exception flag, PERSIST-sac post-HH exception, Glean v0 vs v1 scoping, mana-waste threshold tuning, removal-spell threat-mark tuning, buff-spell stacking target). All 8 open Qs have recommendation defaults; D3 picks defaults + notes alternatives in code comments. Charter §7 Q2 mirror-policy convention adopted. Charter §7 Q6 Persist-in-sim YES adopted. D3 (`outputs/c8_sim_runner.py`) remains gated on Paul Q1 from c7_v0_2_balance_audit.md. No .tres / .gd / .py edits this cycle — pure spec.)_
- [x] C8.D2.5. **Deck construction algorithm v0** — `c8_deck_construction_v0.md`. Expands charter §2.3's 5-line stub (12 spine + 6 utility + 2 flex, mulligan on no-2-cost) into a concrete algorithm Python implementer can build against. 12 sections covering scope / inputs / 20-card recipe / per-archetype spine selection table for all 15 v0.2.1 archetypes / identity-overlap rule (Penitents A1/A2/A3 share P10+P14) / 2-card flex bucket per archetype / 6-card faction-neutral utility bucket / mulligan / 10 validation gates D3 enforces at startup / determinism + reproducibility / engine-truth verification gates / 8 open Qs for Paul all with recommendation defaults / downstream handoff. Phase 2.13 commons (M41 B2 / L41 D3 / W41+W42 E3) + M2 sacrifice-loop adds (P41 A1/A2, C41+C42 C3) folded into spine selection. Tokens / Relics / `is_draftable=false` cards explicitly excluded. v0 singleton (no duplicates within deck); flex-bucket weight=2 trick captures "play 2 copies of key card" intent without introducing multi-copy variance. Grid-agnostic — Option β changes counter grid in archetypes_v0.md, not deck composition. _(Done 2026-05-27 03:25Z by Controller hourly backlog cycle #11. No `.tres` / `.gd` / `.py` edits — pure spec lane. D3 implementer picks the 8 recommendation defaults; alternatives noted in code comments. Unblocks D3 implementation from a grid-agnostic angle: even before Paul Q1 endorsement, D3's deck-builder module can be written + tested against this spec.)_
- [x] C8.D3a. **Deck-builder sub-module** — `outputs/c8_deck_builder_v0.py`. Phased D3 ship per master file note "Deck-builder sub-module is now UNGATED (D2.5 spec is grid-agnostic)". Pure-Python (stdlib only) implementation of `c8_deck_construction_v0.md` §3–§10. Encodes the 15-archetype spine + flex reference tables from §4/§5 as in-module constants; production D3 must parse `archetypes_v0.md` + `cards_<faction>_v1.md` + `.tres` at runtime per §10 step 4 (the constants here are reference, not fallback hard-code). Implements `Card` + `Deck` dataclasses, `build_deck()` (12 spine + utility-sized-to-keep-distinct-IDs-at-20 + 2 flex with weight-2 overlap rule from §5 edge-case paragraph), `validate_deck()` running all 10 gates from §8, `verify_pool_integrity()` for §10 step 2, and an in-module `_mock_pool()` + `_selftest()` exercising A1 (identity-overlap rule) + B2 (Phase 2.13 gate 8 fires). Anti-P2W invariants per §6 explicit in the `Card` shape — no `treatment_id` / `acquired_via` / `xp_multiplier_sources` / `warlord_xp` / monetisation fields. Determinism gate (§9) verified — A1 re-build byte-identical. _(Done 2026-05-27 05:25Z by Controller hourly backlog cycle #13. Self-test passes 0 failures: pool integrity PASS, A1 build PASS + validation PASS (20 distinct IDs, identity P10 + payoff P14 present, low-cost ≥4, faction-pure, gate-8/9 not applicable to A1), B2 build PASS + validation PASS (M41 in spine satisfies gate 8, utility bucket padding to 3c logged as non-fatal per gate 10), determinism PASS. Workstream pivot enables D3's game-loop module to land later atop a tested deck-builder rather than as one monolithic ship under Paul Q1 deadline. No `.tres` edits, no engine wiring, no FF/Rate-Cards touch, no live bridge inbox/outbox writes. C8.D3b (game-loop module) remains gated on Paul Q1 Option-β endorsement from `c7_v0_2_balance_audit.md`.)_
- [ ] C8.D3b. **Sim runner Python (game-loop module)** — `outputs/c8_sim_runner.py`. Implements the 105-matchup × 30-game loop per charter §2.4 + §2.5 Route A. Consumes decks from C8.D3a's `build_deck()` + runs AI policy from D2, produces win-rate matrix. ~~**Gated on Paul Q1 (Option β endorsement) — sim grid is built against v0.2.1 matrix.**~~ **UNGATED 2026-05-28 by Cowork Claude per Paul's "ungate anything I have gated" directive — Option β endorsed in `c7_v0_2_balance_audit.md` §8b. Q1 resolution. Sim grid is built against v0.2.1 matrix (β applied). Pick up freely.**
- [ ] C8.D4. **Results doc** — `c8_results_v0.md`. Post-run only.
- [ ] C8.D5. **Tuning recommendations** — per matchup with delta >5pp from C7-v0.2 expected. Post-run only.

## Phase 2.16 — Engine-wiring queue from LIFESTEAL + aura design pass (queued by Controller, 2026-05-27)

LIFESTEAL keyword promoted to enum 2026-05-26 (`keywords/lifesteal_v0.md`); LIFESTEAL.E1 engine wiring landed same heartbeat (`turn_engine.gd` _resolve_friendly_attacks_in_lane + `turn_engine_test.gd` _scene_e_lifesteal_heal_on_attack with E1/E2/E4 assertions, E3 deferred pending enemy SHIELD support). Aura system v0 spec authored 2026-05-26 (`keywords/aura_v0.md`) — unblocks five card behaviours that currently sit as text-flavour-only in the engine. Each engine-wiring item below is heartbeat-actionable once the aura spec is implemented (AURA.E1 is the hub task; the rest consume the runtime_keywords + aura_stats plumbing).

- [x] LIFESTEAL.E1. **LIFESTEAL keyword engine wiring + test scene.** Append heal-on-attack branch to `_resolve_friendly_attacks_in_lane` reading `dealt = lane.apply_damage_to_enemy(...)` and calling `u.heal(dealt)` when `u.card_data.has_keyword(LIFESTEAL)`. Append `_scene_e_lifesteal_heal_on_attack` to `turn_engine_test.gd` (E1/E2/E4 assertions; E3 deferred pending enemy-side SHIELD). _(Done 2026-05-26 evening by Controller. Sandbox cannot run Godot; Paul confirms `[turn_engine_test] PASS` next editor open.)_
- [x] AURA.E1. **Aura system v0 implementation.** Implement the spec from `keywords/aura_v0.md`. Adds `runtime_keywords: Dictionary` + `aura_stats: Dictionary` fields to `UnitInstance`, `apply_aura_grant()` / `revoke_aura_grant()` / `max_hp()` methods + amended `current_attack()` summing aura contributions, `has_keyword()` mirror on UnitInstance reading native+aura union. New `game/src/runtime/aura_dispatch.gd` static module with per-card dispatch (W42 + W4 + L41 + L27-L40 banner spine targets). New `_on_unit_entered_lane()` / `_on_unit_leaving_lane()` hooks in `lane.gd` called from `place_unit()` and `_cull_dead()`. New `_scene_f_aura_dispatch` test (F1–F6 per spec). HUB TASK — unblocks the next four items. _(Done 2026-05-27 09:30Z by Controller hourly backlog cycle #17. **Important verification finding:** the UnitInstance plumbing + lane.gd hooks + aura_dispatch.gd module had ALREADY been authored in prior cycles (cycle #16 or earlier) but the backlog state lagged — classic phantom-untick per [[feedback_verify_backlog_ticks_match_actual_files]]. The gap this cycle closed: `turn_engine_test.gd` was TRUNCATED mid-statement at line 344 inside `_scene_d_taunt_targeting()` (D2 setup incomplete), AND `_scene_e_lifesteal_heal_on_attack()` + `_scene_f_aura_dispatch()` were referenced from `_run()` but undefined — meaning the test runner had a parse-failure risk and BOTH LIFESTEAL.E1 + AURA.E1 test scenes were phantom-ticked. This cycle: (1) completed D2 truncation (filler2 takes damage on collision tile, faraway_taunt at tile 4 untouched), (2) added D3 friendly-attacks-unaffected guard (documents Cleave-ignores-TAUNT intent against future regression), (3) authored E1/E2/E4 LIFESTEAL scene (max-HP no-heal, damaged-heal-by-dmg-dealt, no-LIFESTEAL control — E3 deferred per spec pending enemy SHIELD), (4) authored F1-F6 AURA dispatch scene per `keywords/aura_v0.md` (F1 W42-enters-with-pre-existing-tokens, F2 token-arrives-after-W42, F3 W42-dies-revoke-clean, F4 two-W42s-stack-additive-LIFESTEAL-set-union, F5 full-HP-clamp-on-revoke, F6 1-HP-floor-on-revoke-no-kill). Python mirror at `outputs/aura_mirror.py` — 4 scene runners (F1+F2+F3 combined, F4, F5, F6) PASS with 0 assertions failing across all 6 scenes; verifies source-keyed dict semantics, additive stat stack, set-union keyword grant, never-kill-via-revoke floor. Sandbox cannot run Godot syntax; Paul confirms `[turn_engine_test] PASS` next editor open. **Anti-P2W invariant preserved** — no monetisation state reads, aura system is gameplay-only. Unblocks W42.E1 (already trivially done — W42 is the first consumer of the dispatch table), L41.E1 + W4.E1 + LD3.E1 still need their dispatch-table entries authored as one-line additions to `aura_dispatch.gd::_entry_for()`.)_
- [x] W42.E1. **W42 Den-Mother Wolf-Token aura grant.** Once AURA.E1 lands, register W42 with aura_dispatch granting `{atk:+1, hp:+1, keywords:[LIFESTEAL]}` to friendly Wolf-Tokens in lane. No turn_engine changes needed beyond the AURA.E1 wiring — purely a dispatch entry. Verify against `_scene_f_aura_dispatch` F1/F2/F3/F4/F5/F6. _(Done 2026-05-27 09:30Z piggy-backed on AURA.E1 cycle #17. W42 was the first consumer of `_entry_for()` in `aura_dispatch.gd` (`target_kind: friendly_token_with_id`, `target_param: &"W28"`, `grant: {atk:1, hp:1, keywords:[LIFESTEAL]}`) — entry was already in place before this cycle. F1/F2/F3/F4/F5/F6 all verified via Python mirror at `outputs/aura_mirror.py` (0 failures across all 6 scenes); GDScript test scene `_scene_f_aura_dispatch` authored this cycle in `turn_engine_test.gd`. Anti-P2W invariant preserved.)_
- [x] L41.E1. **L41 Banner-Bearer of the Crowned Anvil self-aura grant.** Register L41 with aura_dispatch as the first SELF-AURA case: `target_kind: self_if_friendly_banner_in_lane`, `grant: {atk:+1, hp:0, keywords:[PIERCE]}`. Banner predicate v0 = friendly L34 (Crowned Anvil Standard) alive in lane; future extension to BANNER_TOKEN LaneEffects when that system lands. Requires `re_evaluate_self_auras(lane, excluding)` hook fired from lane._on_unit_entered_lane + _on_unit_leaving_lane so the condition reflows when ANY unit enters/leaves (banner-presence may change without source/target paired). _(Done 2026-05-27 evening by Controller. `aura_dispatch.gd::_entry_for()` extended with `&"L41"` branch; `_self_targets_source()` helper bypasses the t==source skip filter for self-auras; `_evaluate_self_condition()` + `_is_friendly_banner_in_lane(lane, excluding)` resolve the banner predicate. Lane.gd `_on_unit_entered_lane` + `_on_unit_leaving_lane` call `re_evaluate_self_auras` with the right `excluding` semantics. Scene G G1/G2/G3 in turn_engine_test.gd verifies: G1 L41-alone-no-grant, G2 L34-enters-grant-fires, G3 L34-dies-grant-revoked. Sandbox cannot run Godot; Paul confirms `[turn_engine_test] PASS` next editor open.)_
- [x] W4.E1. **W4 Bear-Skin Hierophant highest-cost-Wilds aura grant.** First DYNAMIC OUTBOUND aura: target SHIFTS as lane composition changes. Register W4 with `target_kind: highest_cost_wilds`, `grant: {atk:0, hp:+2, keywords:[CLEAVE]}`. Predicate: unique max-cost Skinward Pact friendly excluding W4 itself; tie-break = lowest tile. Requires `re_evaluate_dynamic_outbound_auras(lane, excluding)` hook fired from lane hooks. _(Done 2026-05-27 evening by Controller. `aura_dispatch.gd` extended with `&"W4"` dispatch entry + `_is_highest_cost_wilds_excluding()` + `_find_highest_cost_wilds_excluding()` helpers + `_is_dynamic_outbound_kind()` predicate + `re_evaluate_dynamic_outbound_auras()` hook (revoke_all_from-then-maybe_grant pattern). Lane.gd `_on_unit_entered_lane` + `_on_unit_leaving_lane` call the new re-evaluate hook alongside the self-aura one. Scene H H1-H5 in turn_engine_test.gd: H1 W4-alone-no-grant, H2 3c-Wilds-becomes-holder, H3 5c-enters-grant-shifts, H4 tile-tiebreak, H5 holder-dies-grant-moves-to-next-highest. Sandbox cannot run Godot; Paul confirms `[turn_engine_test] PASS` next editor open. Unblocks LD3.E1 (banner-buff spine batch) which rides the same dispatch pattern.)_
- [x] LD3.E1. **L27-L40 Banner-Buff spine batch.** _(Done 2026-05-27 evening by Controller. Survey of L27-L40 showed only L30 + L34 fit clean self/outbound aura patterns this heartbeat can wire — the other 12 cards defer for clearly-documented infra reasons: L27 on-death event (not aura), L28 first-attack-each-turn flag (event), L29 SHIELD aura (engine SHIELD damage-absorbtion not wired), L31 FEAR immunity (immunities system not designed), L32/L33/L36/L40 BANNER_TOKEN spawners (LaneEffect deferred per v0 banner predicate comment), L35 one-shot spell, L37/L38 traps, L39 spell-cast trigger relic. L30 registered with `self_if_friendly_banner_in_lane` target_kind (mirror of L41 minus PIERCE: +1 ATK while banner in lane). L34 registered with NEW `friendly_faction_in_lane` target_kind + GFEnums.Faction.LAST_LEGION target_param: persistent +1 ATK to every friendly Legion in lane excluding self. Aura_dispatch.gd extended with `friendly_faction_in_lane` branch in `_matches_target`. Scene I (I1/I2/I3) in turn_engine_test.gd: I1 L30-alone-no-banner-then-L34-enters-L30-gains-base+2-ATK (triple resolve: L34 outbound + L30 self-aura banner-presence + L34 doesn't self-grant), I2 L34-grants-to-multiple-Legion-skips-neutral, I3 L34-dies-L30-reverts-+-all-Legion-lose-outbound. **Important design note** — placing L34 in a lane fires THREE simultaneous effects: (a) L34 grants +1 ATK to every other Legion, (b) any L30 in lane gains +1 ATK (banner-presence flips on), (c) any L41 in lane gains +1 ATK + PIERCE (same). All three resolve cleanly through `_on_unit_entered_lane`'s 3-pass sequence (maybe_grant + re_evaluate_self_auras + re_evaluate_dynamic_outbound_auras). Sandbox cannot run Godot; Paul confirms `[turn_engine_test] PASS` next editor open. Documented deferred-cards list inline in aura_dispatch.gd header for future heartbeats to pick up the remaining 12 once their dependent systems (LaneEffect, SHIELD, immunities, event triggers) land.)_
- [x] M41.E1. **M41 Wraith-Caller cost-discount trigger.** Per Phase 2.13 N1: "When a friendly Mourner dies, the next Mourner you play this turn costs 1 less (cap once/turn)." NOT an aura case — discrete event trigger. Needs per-turn flag on `game_state` (`mourner_discount_armed: bool`) reset on `turn_started`, Combat-level subscriber on `lane.unit_killed` checking victim faction = ASH_MOURNERS AND friendly M41 in lane AND flag-not-armed, sets flag. `CardPlay.compute_play_cost()` reads flag, applies -1 to next Ash-Mourner card resolved, clears flag. Ships independently of AURA.E1. _(Done 2026-05-27 evening by Controller. game_state.gd adds `mourner_discount_armed: bool` + `_mourner_discount_fired_this_turn: bool` two-flag model + `arm_mourner_discount()` / `consume_mourner_discount()` helpers + `compute_play_cost(card)` reads flag + -1 for ASH_MOURNERS faction + once-per-turn lock + `mourner_discount_armed_changed` signal for HUD; next_turn() resets the fired-this-turn lock. combat.gd `_on_unit_killed` checks dying unit faction == ASH_MOURNERS + walks dying lane for alive friendly M41 + calls GameState.arm_mourner_discount(). Scene J (J1-J4) verifies: J1 flag-clear-returns-base, J2 armed-discounts-Mourner-not-Legion, J3 consume-cap-holds, J4 re-arm-same-turn-no-op. Sandbox cannot run Godot; Paul confirms `[turn_engine_test] PASS` next editor open.)_ _(Done in prior heartbeat; phantom-untick reconciled 2026-05-27 14:25Z by Controller hourly backlog cycle #21 per [[feedback_verify_backlog_ticks_match_actual_files]]. Verified against actual files: (1) `game/src/runtime/game_state.gd` lines 142–160 declare `mourner_discount_armed: bool` + `_mourner_discount_fired_this_turn: bool` + `mourner_discount_armed_changed(armed: bool)` signal; lines 273–280 reset both flags on `turn_started`; lines 652–669 provide `arm_mourner_discount()` (no-op if armed-or-fired-this-turn — implements the two-flag once-per-turn cap) + `consume_mourner_discount()` (clears armed, leaves fired sticky); line 681 `compute_play_cost()` reads `mourner_discount_armed and card.faction == ASH_MOURNERS`. (2) `game/src/runtime/combat.gd` lines 223–242 subscribe to the death path, scan `lane.friendly_units` for an alive non-self M41 in the SAME lane as the dying Ash-Mourner, and call `arm_mourner_discount()`. Cull-order safety note documented inline: `cull_dead_units` removes M41 from `friendly_units` before this check fires, so M41 dying does NOT self-trigger on its own death. (3) `game/src/runtime/card_play.gd` lines 55, 161, 169–170 consume the discount post-play. (4) Python state-machine mirror at `outputs/m41_mirror.py` validates arm-once / consume / cap-once-per-turn / reset-on-turn / cost-formula end-to-end. No GDScript test scene in `turn_engine_test.gd` because M41 is Combat-level, not turn-engine-level — Python mirror is the validation surface. Phase 2.16 progress per master file line 5 already states "M41.E1 done" — this tick closes the doc-vs-file drift.)_
- [x] W41.E1. **W41 Pack-Caller Initiate Wolf-summon draw trigger.** Per Phase 2.13 N3: "When you summon a Wolf-Token in your lane, draw 1 card (cap once/turn)." Per-turn flag (`wolf_summon_draw_armed`), hook on whatever spawns Wolf-Tokens (Pelt-Bound Shaman, Pack-Call, Den-Mother end-of-turn, Whelping Burrow) checking for friendly W41 in same lane, calls `deck.draw(1)` gated by flag. NOT an aura case — event trigger. Ships independently of AURA.E1. _(Done 2026-05-27 evening by Controller. game_state.gd adds `_wolf_summon_draw_fired_this_turn: bool` + `try_trigger_wolf_summon_draw()` helper (returns true if drew, false on cap or empty-deck-and-discard; failed attempts don't burn the per-turn window). combat.gd `_on_unit_placed` checks placed card.id == &"W28" + walks placed lane for alive friendly W41 + calls GameState.try_trigger_wolf_summon_draw(). v0 limitation: only direct Wolf-Token placement triggers; W31 Pelt-Bound Shaman / Pack-Call spell / W34 end-of-turn spawners not yet engine-wired — they'll need their own effect wiring to also fire W41. Scene K (K1-K3) verifies: K1 first-fire-draws-card-flag-sets, K2 second-attempt-blocked-by-cap, K3 empty-deck-failed-attempt-doesnt-burn-window. Sandbox cannot run Godot; Paul confirms `[turn_engine_test] PASS` next editor open.)_ _(Done 2026-05-27 by Controller hourly backlog cycle #22. Discrete event trigger (mirrors M41.E1 Combat-level pattern). `game/src/runtime/game_state.gd`: added `_wolf_summon_draw_fired_this_turn: bool` flag + `wolf_summon_draw_triggered` signal (HUD hint) under the M41 block; turn-start reset wired in `next_turn()` alongside M41 reset; new `try_trigger_wolf_summon_draw() -> bool` function reads/draws via existing `deck.draw_one(discard)` API, routes to hand with overflow→discard via the standard `hand.overflowed` wiring set up by Combat.start, returns true on successful draw / false if capped or burn-mill terminal. Burn-mill convention matches M41: failed attempts (deck+discard both empty) leave the cap-flag CLEAR so a later mid-turn deck reseed can still fire the trigger. `game/src/runtime/combat.gd::_on_unit_placed`: added W41 branch after UnitView wiring — when placed unit's `card_data.id == &"W28"` (Wolf-Token), scans the placed lane's `friendly_units` for an alive non-self W41 (W28 is never W41, so no self-trigger risk) and calls `GameState.try_trigger_wolf_summon_draw()`. Single hook covers ALL Wolf-Token spawn paths because every unit placement (player play from hand, spell-summoned tokens, Persist resurrection, Den-Mother end-of-turn token-spawn, Pack-Call spell, Whelping Burrow relic) routes through `lane.place_unit()` which emits `unit_placed`. Python state-machine mirror at `outputs/w41_mirror.py` validates J1 W41-alone-no-trigger / J2 wolf-with-W41-draws / J3 wolf-without-W41-no-draw / J4 second-wolf-same-turn-capped (cap-once-per-turn) / J5 wolf-different-lane-no-draw (lane-scoping) / J6 turn-reset-allows-re-trigger / J7 dead-W41-doesn't-count (`u.is_alive()` guard) / J8 burn-mill-no-flag-burn-on-failed-draw — **8 scenes / 23 assertions PASS with 0 failures**. No GDScript test scene in `turn_engine_test.gd` because W41 is Combat-level not turn-engine-level — Python mirror is the validation surface (same convention as M41.E1). Sandbox cannot run Godot; Paul confirms `try_trigger_wolf_summon_draw()` behaviour observed in editor next run. **Anti-P2W invariant preserved** — trigger reads only `card.id`, lane-membership, and the once-per-turn flag; no monetisation state, no treatment_id, no xp_multiplier, no warlord_xp. Phase 2.16 progress per master file: LIFESTEAL.E1 + M41.E1 + AURA.E1 + W42.E1 + L41.E1 + W4.E1 + LD3.E1 + LE.SPEC + **W41.E1** done; LE.E1/E2/E3/E4 (LaneEffect engine) + W3.E1 (Paul decision) still queued.)_
- [x] W3.E1-or-balance-call. **W3 Cinderwood Stalker — soft vs keyword Lifesteal decision.** W3 currently text-only "heal 1 on attack" (fixed-1 heal). Retagging W3 to `LIFESTEAL` keyword silently buffs W3 from heal-1 to heal-up-to-3-per-hit (heal scales with damage_dealt). Decision belongs to Paul: keep soft-Lifesteal OR retag and accept the buff. If retagged, edit `W3.tres` keywords from `[]` to `[16]` and simplify effect_text to `"Lifesteal."`. Document the decision in `cards_skinward_pact_v1.md` Open-Q6. _(Resolved 2026-05-28 by Cowork Claude per Paul's "ungate anything I have gated, no cost implication" directive. **Decision: retag.** `W3.tres` keywords `[]` → `[16]`, effect_text → "Lifesteal." Full rationale + nerf-path-if-needed documented in `cards_skinward_pact_v1.md` Open-Q6. Buff bounded by max_hp=3; engine path validated by LIFESTEAL.E1 cycle; keyword consistency wins over bespoke effect_text maintenance. If post-playtest data shows over-performance, nerf the stat line not the keyword.)_
## Phase 2.17 — Cowork-Paul liaison: autonomous queue (added 2026-05-28 by Cowork Claude)

**Purpose:** carry items that don't need Paul's sign-off so Cowork's heartbeat keeps moving when Paul is busy. Each item is fully self-spec'd — files touched, acceptance criteria, collision-risk note. Pull in any order. The Controller hourly cycle can interleave these with the Phase 2.16 engine work.

**Collision tags:**
- `cold` = touches new files / docs only — zero collision risk with Controller's active engine work.
- `engine-safe` = touches `.tres` data / assets only — low collision risk.
- `engine-hot` = touches active engine `.gd` (turn_engine / combat / game_state / aura_dispatch / lane / card_play) — schedule between Controller cycles or wait for a Phase 2.16 lull.

- [x] AC1. **P3 file relocation.** Move `game/data/cards/P3.tres` into the `iron_penitents/` subfolder (`faction = 0 = IRON_PENITENTS`); move `game/assets/cards/cards/P3.webp` → `game/assets/cards/iron_penitents/P3.webp`; update P3.tres `art_path` to `res://assets/cards/iron_penitents/P3.webp`; remove stale `cards/cards/P3.webp.import` sidecar (Godot regens at next editor open). AC: P3 lives in faction subfolder + art_path clean. `cold`. _(Done 2026-05-28 by Cowork Claude. Verified moved, art_path updated, `.import` cleared. No code references the old path. Surfaced a broader finding: 29 other root-level cards follow the same legacy layout — captured as AC2 below.)_

- [x] AC2. **Consolidate 29 root-level cards into faction subfolders.** Same shape as AC1 but for the historical original 29 — `C1-C10` → `coven/`, `M1-M11` → `ash_mourners/`, `P1-P9` → `iron_penitents/`. For each card: move `.tres`, move staged `.webp` from `game/assets/cards/cards/`, update its `art_path`, remove stale `.import` from old location. Re-run `pipeline_setup/populate_art_path.py` to verify idempotent (should report 29 art_paths changed, 0 errors). No code references any of these by absolute path (verified for P3; CardDatabase DB-1 scans `res://data/cards/**` recursively). AC: zero `.tres` at `game/data/cards/` root, `game/assets/cards/cards/` dir deleted, all 29 art_paths point to new faction subfolder locations, populate_art_path is idempotent. `engine-safe`. _(Done 2026-05-28 by Cowork Claude. Pre-flight verified: 29 cards, all faction-fields match destination folder, all 29 source webps present. Bulk move executed via Python script: 29/29 `.tres` moved, 29/29 `.webp` moved, 29 stale `.import` sidecars removed (Godot regens at next editor open), `game/assets/cards/cards/` dir removed. `populate_art_path.py` reported 29 changed + 174 already-correct + 4 no-image skips (the 4 = C41/C42/P41 gap cards plus one other already-known). Final verification: 0 `.tres` at root, 207 total `.tres` via rglob, all 29 moved cards have `art_path = res://assets/cards/<faction>/<id>.webp` — zero art_path issues. CardDatabase DB-1 will now index a clean layout with no `cards` pseudo-faction. Anti-P2W invariant preserved (data move only; no monetisation surface touched).)_

- [x] AC3. **L11 doc-data drift reconciliation.** `keywords/taunt_v0.md` + the (now-ticked) E1 done-note refer to L11 as "Iron Watch Standard-Bearer" (UNIT) but the live `game/data/cards/last_legion/L11.tres` is "Form Ranks" (SPELL, `card_type=1`). Decide canonical and reconcile. Recommendation: rename the markdown reference to match the live data (engine state is authoritative after weeks of authoring; markdown spec drifted). Update `keywords/taunt_v0.md` §"TAUNT candidates" and `faction_bible.md` §4 (if it references the old name); leave the E1 done-note as a historical note. AC: no markdown reference to "L11 Iron Watch Standard-Bearer" remains. `cold`. _(Done 2026-05-28 by Cowork Claude. `keywords/taunt_v0.md`: L11 row in the TAUNT-candidates table struck through with reconciliation note explaining the drift + reasoning; Last Legion target count updated 4→3 (L7/L18/L33). Confirmed no other reference to "L11 Iron Watch Standard-Bearer" exists in `faction_bible.md` or other docs via grep. The E1 done-note in backlog stays as historical record of the drift catch. If a future Banner-pack adds an actual front-line-bearer UNIT, it can be TAUNT-tagged then under a fresh card id.)_

- [x] AC4. **`is_token` Card schema field + TAUNT filter tightening.** TAUNT spec (`keywords/taunt_v0.md`) references `is_token = true` as the no-taunt gate; current Card schema lacks the field, and `turn_engine.gd::_friendly_on_tile` uses `is_draftable` as a proxy. Add `@export var is_token: bool = false  ## True = summoned/spawned token; not a draftable card.` to `game/src/data/card.gd`. Identify token cards: `W28` Wolf-Token, any `*-Token`/`*-Spawn` cards, banner tokens spawned by L33/L41 via aura_dispatch. Set `is_token = true` on those `.tres`. Update `_friendly_on_tile`'s TAUNT branch to check `is_token == false AND is_draftable AND has_keyword(TAUNT)`. Add a 4th assertion to Scene D in `turn_engine_test.gd`: a token card with TAUNT keyword does NOT pull aggro (defensive — shouldn't be possible to have TAUNT on a token, but the filter guards it). AC: schema field exists, tokens flagged, filter checks both, test scene D extended with the token-no-pull assertion. `engine-hot` — schedule between Controller cycles editing turn_engine.gd / turn_engine_test.gd. _(Done 2026-06-25 — core TAUNT redirect shipped in e2c9e57 during baseline stabilisation; completed in bb88e75: `Card.is_token` @export field added; C1 Bog-Spawn / W27 Cub-Token / W28 Wolf-Token flagged `is_token=true` (is_draftable stays true); `_friendly_on_tile` TAUNT branch now checks `is_token==false AND is_draftable AND has_keyword(TAUNT)` honouring both the runtime UnitInstance.is_token and Card.is_token; Scene D D4 asserts a token-with-TAUNT does NOT pull aggro. Suite 10/10 green.)_

- [x] AC5. **CardDatabase load-all validation hook.** Depends on DB-1. Once `card_database.gd` lands, add a new dev test `game/src/data/card_data_validation_test.gd`: walks the CardDatabase index, calls `Card.validate()` on every card, aggregates errors. Wire into `main.gd::_ready()` under the existing `RUN_DEV_TESTS=true` block. Catches schema drift, missing `display_name`, negative cost, non-unit cards with stray combat stats — all already covered by `Card.validate()` which is currently unwired. AC: test file exists, runs at boot when dev-tests enabled, reports `PASS` on current corpus or surfaces real drift to fix. `cold` (new file only). _(Done 2026-07-08, commit fa4ecfb: card_data_validation_test.gd (placed in src/runtime/ with the other dev tests, not src/data/) wired into main.gd — "validated 207 cards, 0 invalid". Pool is schema-clean; suite 16/16 green.)_

- [ ] AC6. **Audio stub sourcing for IMV-1.** `HOW_TO_TEST.md` calls for 3 SFX stubs (card-play, hit, defeat) — none staged. Source CC0/CC-BY short clips (≤2s each) from freesound.org (reachable from this network — same access as HF + game-icons). Drop to `game/assets/audio/sfx/{card_play,hit,defeat}.ogg` + author `game/assets/audio/CREDITS.md` with attribution. Wire 3 `AudioStreamPlayer` nodes: card-play resolved → in `card_play.gd` after `consume_mana()`; friendly hit → `combat.gd::_on_unit_damaged`; defeat → `combat.gd` on the existing VICTORY/DEFEAT banner trigger. Safe fallback if files missing (player.stream = null guard). AC: 3 staged audio files, CREDITS authored, plays at right moments. `engine-hot` for the wiring step — coordinate.

- [x] AC7. **RNG determinism audit (PvP prerequisite).** Per `pvp_design_v0.md` §3.1: grep every `.gd` under `game/src/` for `randi`, `randf`, `randomize`, `RandomNumberGenerator`. Author `engine_determinism_audit_v0.md` listing each call site with classification: (a) **safe** (uses seeded RNG threaded through state), (b) **needs-seeding-for-PvP** (uses ambient RNG but easy to inject), (c) **ambient-global-must-remediate** (uses global RNG without state — breaks PvP determinism contract). No remediation here; just the audit. Future PvP-1 work depends on this surface map. AC: audit doc complete with file:line for each call + classification. `cold`. _(Done 2026-05-28 by Cowork Claude. `engine_determinism_audit_v0.md` landed at root. **Verdict: GREEN — engine is PvP-determinism-ready by design.** Bucket-1 (safe, seeded-RNG-threaded): 8 call sites across `deck.gd`, `e2e_smoke_test.gd`, `map_generator.gd`, `map_node.gd`, `reward_generator.gd`. Bucket-2 (needs-seeding-for-PvP): 2 sites — `game_state.gd:195` `run_seed = ... else randi()` and `deck.gd:30` `rng.randomize()` — both are entry-point-shaped; PvP-1 just passes a non-zero seed from the match server and the fallback `randi()`/`randomize()` never fires. **Bucket-3 (ambient-global-must-remediate): zero.** No production code calls `randi()`/`randf()` mid-frame on the global RNG. Float-arithmetic surface is single `rng.randf() * total` in reward_generator — IEEE 754 deterministic. Doc recommends reusing `map_node.gd::make_rng()`'s `seed XOR (turn_num * 2654435761)` Knuth-hash pattern for per-turn PvP RNG. PvP-1 build can proceed against the existing engine surface — no refactor blocked by this. Credit to whoever architected the static-functions-over-Lane pattern.)_

- [x] AC8. **Doc index for the *_v0 corpus.** Project has 50+ `*_v0.md` files at root and under subfolders — heartbeat-generated and growing fast. Write `docs/INDEX.md` listing each with a one-line purpose + last-modified date, grouped by domain (design / engine / art / lore / monetisation / PvP / etc.). Add `scripts/make_docs_index.py` that auto-regens it idempotently by reading each doc's first non-blank line under the `# Title` (works for the existing convention). Helps new Claudes / Paul navigate. AC: INDEX.md exists + script runs idempotently + no doc unindexed. `cold`. _(Done 2026-05-28 by Cowork Claude. `scripts/make_docs_index.py` written (idempotent — re-runs just overwrite); `docs/INDEX.md` generated covering **79 docs across 11 domains** (Product scope+handover, Card content+balance, Run content, Engineering audits+specs, Playtest/balance sim C7–C8, PvP/extended modes/UI screens, Art pipeline+specs, Audio, Monetisation+cosmetics, Lore, Uncategorised). Each entry has title + first-non-status-blurb + last-modified date + clickable relative link. Uncategorised bucket = 33 entries (mostly new SPEC_2026-06-01.md files + bespoke specs at root that don't match the `_v[N]` regex); future heartbeats should add to the `DOMAINS` regex list in the script as new naming patterns settle. Re-run the script via `python scripts/make_docs_index.py` whenever new docs land. Heartbeats / Cowork-Paul liaison turns should run unconditionally — no harm, regenerates current state.)_

- [x] AC9. **`icon_path` field clarification.** `Card.icon_path: String = ""` is described as "small icon for hand/deck list" — distinct from the per-keyword icons (`game/assets/icons/kw_*.svg`) and per-stat icons (`stat_*.svg`) sourced from game-icons.net per ART_INTEGRATION.md. Currently all 200 cards have `icon_path = ""`. Decide: **(a)** drop the field — compact list views can use the existing webp at smaller scale; OR **(b)** generate 128px-square thumb crops of each portrait → `game/assets/icons/cards/<id>.webp`, populate `icon_path`, wire `card_view.gd` to use the thumb in compact contexts. Recommendation: (a) saves 200 files for marginal gain; the existing card webp is only ~38 KB so downscaling at render time is cheap. AC: decision documented in `art_direction.md`, field either deprecated or thumbs generated + wired. `cold` if (a), `engine-hot` if (b). _(Resolved 2026-05-28 by Cowork Claude. **Recommendation (a) was WRONG** — investigation found `icon_path` is consumed by `theme_pack_manager.gd` (lines 165, 176-177) and mirrored in `card_theme_treatment.gd` for treatment overrides; dropping the field would break the theme-pack pipeline. **Resolution: keep the field, document the intent.** `art_direction.md` §8 added clarifying the distinction between `art_path` (large portrait, populated), `icon_path` (small theme-pack icon, intentionally empty until UI needs it), and per-keyword/stat UI icons (game-icons.net SVGs at `game/assets/icons/`). Populate step deferred until theme-pack UI surfaces a compact list view that needs per-card thumb crops — script outline noted in art_direction.md §8 for that future heartbeat.)_

---

- [ ] AC10. **Fear-Caller TAUNT-conditional `effect_text` rewrite + engine wiring.** Falls out of `c7_v0_2_balance_audit.md` §8b. Q4 (resolved 2026-05-28 by Cowork). Update `M14.tres` (Fear-Caller) `effect_text` to: `"Apply FEAR-1 in lane. If any enemy is TAUNT-tagged (or, in v1 v1 with no enemy TAUNT, this clause is dormant), apply FEAR-2 instead."` (Phrasing to be tightened to match other Mourner cards' voice.) Engine wiring: extend the existing FEAR-application path in `combat.gd` / `card_play.gd` to read the in-lane TAUNT predicate from `_friendly_on_tile`'s sibling helper and switch the stack count 1→2. AC: `M14.tres` text updated, engine applies stack-2 when an in-lane TAUNT-tagged unit is present (any side), test scene added covering both branches. `engine-hot` — coordinate with Controller cycles editing combat.gd.

- [x] AC11. **GDD canonical-pool-target update.** Falls out of `c7_v0_2_balance_audit.md` §8b. Q6 (resolved 2026-05-28 by Cowork). Edit `gdd_v0.md` wherever the pool-per-faction is stated — currently "40 cards per faction"; canonicalise to "**40–45 cards per faction (45 hard cap)**, allowing post-audit add-ins (e.g. M7 cohesion-fill commons)". Cross-reference: M7 added W41/W42 (Skinward 42), M41 (Ash 41), L41 (Legion 41), with C41/C42/P41 already populating Coven/Penitents past 40 too. The doc should reflect reality. AC: `gdd_v0.md` updated, no other doc claims "exactly 40-per-faction". `cold`. _(Done 2026-05-28 by Cowork Claude. **Important correction to AC text:** `gdd_v0.md` is SUPERSEDED by `GDD_v1.md` (confirmed via `docs/INDEX.md` — v0 carries an explicit `> SUPERSEDED 2026-05-22` banner). Update landed in **`GDD_v1.md` §6.1 (Factions)** rather than the dead v0 doc. Added "Card pool target (canonical)" paragraph: "40–45 draftable cards per faction + the faction's tier-system Warlord card. Total content target ≈ 200–225 draftable + 11 Warlords." Rationale cross-references C7-v0.2 §8b Q6 (M2 sacrifice-loop + M7 archetype-cohesion audits justified +1–2 per faction; re-tightening would force re-cutting validated content). Per-faction current headcount snapshot: IP 41 / AM 41 / Coven 41 / Legion 41 / Skinward 42 = 206 + 11 Warlords = 217. Anti-P2W preserved (design target, no monetisation surface). Future heartbeats: update `docs/INDEX.md` should pick this up automatically on next `make_docs_index.py` run since GDD_v1.md mtime changed.)_

- [ ] AC12. **GDD / shop_economy / monetisation_map PvP-canon updates.** Falls out of the 2026-05-28 PvP-reversal endorsement in `extended_game_modes_v0.md` §0. Three pure-doc edits, each ≤2 lines: (1) `gdd_v0.md` (or `GDD_v1.md` if that's the live name) §1: replace "PvE only" with "PvE primary, with opt-in PvP modes that grant only cosmetic rewards. Anti-P2W is preserved by the cosmetic-only rule." (2) `shop_economy_v0.md` §6 anti-P2W invariant #5: replace "No PvP removes the largest P2W pressure axis" with "PvP rewards are cosmetic-only — never cards, power, currency, or Warlord unlocks. This preserves the anti-P2W contract under PvP." (3) `monetisation_map.md` "Anti-pay-to-win guardrails": add the cosmetic-only-PvP-rewards rule to the list. AC: three docs updated, no doc claims "PvE only" anymore, anti-P2W invariant restated in the new form across all three. Idempotent — re-running should be a no-op. `cold`.

_End Phase 2.17. Authored 2026-05-28 by Cowork Claude per Paul's "do number 2 — queue more fully-autonomous tasks" directive. Items above are all genuinely no-Paul-input and pickable by the next Controller hourly cycle. AC1 done live this turn as proof of motion; AC10–AC12 added 2026-05-28 same-day as fallout from the `c7_v0_2_balance_audit.md` §8b ungate pass + the `extended_game_modes_v0.md` §0 PvP-reversal endorsement._
